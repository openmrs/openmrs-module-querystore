<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!--
            The query store module does not own any tables in the OpenMRS transactional database.
            Its read-side projection lives in an external backend (Elasticsearch, per ADR 3).
            This file is kept as a placeholder for future Hibernate mappings if they are needed.
        -->
    </session-factory>
</hibernate-configuration>

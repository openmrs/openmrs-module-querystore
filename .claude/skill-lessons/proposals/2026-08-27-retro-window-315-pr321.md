# proposals · retro window 2026-08-27 (1 new record: #315 / PR 321) · drafted 2026-08-27
status: **REFUTED AND APPLIED** 2026-08-27. Step 5 ran (fresh general-purpose agent, read-only).
        Outcome: P1 applied revised; P2's EDIT applied but its stated justification REFUTED — do not
        reuse it; P3 parked; **P4 PROMOTED by the refuter** out of the parked list and applied at
        `harden`:185; P5 parked, and covered by `pr-harden`:230 rather than by the project CLAUDE.md
        line cited here; P6/P7 report-only, every limb verified. The Step 4 arithmetic below ("net
        growth is paid for by P2's deletion") is FALSE and was blocked. See `REJECTED.md`, the
        2026-08-27 second-window block, which supersedes this file wherever they differ.

## Window
One un-retroed record: `2026-08-27-chartsearchai-315.md` (resolve-ticket + harden + pr-harden,
#315 / PR 321, converged, 2 pr rounds / 5 harden cycles). `2026-08-27-chartsearchai-317.md` was read
by the 2026-08-27 retro (REJECTED.md's last block). Linter: 10 files, 0 findings.

Precedence adopted for this pass, from REJECTED.md 2026-08-26: the anti-pattern "don't retro a single
record" governs the PASS and Step 3's clauses govern a LESSON; the 2026-08-27 pass applied one lesson
from a single-record window because the CORPUS count reached 2. So this pass applies only what two
records corroborate, and parks clause-2-only single-record lessons.

---

## P1 · ADD — a passing check may have examined nothing (2 records)
**Target:** `harden/SKILL.md`, the Phase 1 paragraph at :53-67 ("The stand-in is not always a stub…
Ask of any clean or zero result what its inputs could not have produced… And of a STABLE result, ask
what the repeats could not have varied."). Appended as a third limb to that existing home; no new home.

**Corroboration (clause 1, two records; and clause 2 within each):**
- `2026-08-26-openmrs-module-chartsearchai-315.md` `[r4]`: "round 3's CAPTURE_DONE fix wrote the marker
  unconditionally: an arm that captured nothing read as a clean, empty A/B, exit 0 · blocking"
- `2026-08-27-chartsearchai-315.md` `[harden c3]`: "`ArchitectureGuardTest` passed 5/5 on a wrong source
  root — it WALKS, so it scanned nothing and reported no violations. · cost: 1 cycle"
- same record `[harden c4]`: "That fix covered 4 of its 5 rules; the fifth walks its own directory and
  returned silently. Then: existence alone was not equivalent to the canary, because the sibling omod
  module carries the same package path. · cost: 2 cycles"

**Proposed text (appended to that paragraph):**
> **And of a PASSING check, ask what it actually examined.** A check that discovers its own subject — a
> source or class-file walk, a directory scan, a script that reports on its own captured output — returns
> the same clean result whether the subject was compliant or absent. Two runs: a walking architecture
> guard passed every rule on a wrong source root, having scanned nothing, and a capture script wrote its
> done-marker unconditionally, so an arm that captured nothing read as a clean, empty A/B with exit 0.
> Make an empty discovery FAIL, and pick the thing you assert was found by what only the intended root
> holds — the same run found that asserting the root merely exists is not equivalent, because a sibling
> module carried the same package path.

**Prune (Step 4):** retires nothing; it appends to a paragraph that already asks the same question of a
measurement's input population and of a stable result's repeats, and supplies the mechanical remedy that
limb currently lacks. Net growth is paid for by P2's deletion in the same commit.

---

## P2 · PRUNE — a superlative this record falsifies (deletion, no new claim)
**Target:** `pr-harden/SKILL.md`:220 — "**For a guard over TEXT or SHAPE,** its weakest point is the gap
between the property it means and the string it matches, and that gap is invisible from the assertion's
own side."

**Measurement that retires it** (`2026-08-27-chartsearchai-315.md`, six guard defects in one PR, `[pr r2]`):
"Six distinct guard defects in one PR; this is the only one where the assertion measured the wrong
PROPERTY rather than looking in too small a WINDOW." So five of the six were not the named weakest point.
"its weakest point is" is also the superlative grammar `harden`:274 and `pr-harden`:224 already flag.

**Proposed edit:** "its weakest point is the gap" → "one gap is". Per Step 4, delete the unsupported
clause rather than replace it with a better-sounding one; the window axis it would be replaced BY is
parked as P4 below, on one record.

---

## P3 · ADD — a base measured in an earlier run is not a base (candidate; 2 records)
**Target:** `harden/SKILL.md`:63-67, the "STABLE result / repeats" limb, as a second sentence.

**Corroboration claimed:**
- `2026-08-26-openmrs-module-chartsearchai-315.md`: "'n=3 byte-identical repeats establish stability' ->
  consecutive repeats measure KV-CACHE stability, not answer stability… Every prompt figure in the first
  5 harden cycles was weaker than it looked · cost: 2 rounds" (this limb is ALREADY SHIPPED as
  `harden`:63-67)
- `2026-08-27-chartsearchai-315.md` `[pr r1]`: "That reviewer measured a DIFFERENT base for the same cell
  than the author did. Three runs on record against the unchanged prompt, three different bases, each
  stable within its own run — so '3/3' against that cell was never safe to publish. Now recorded as
  unsettled." · cost: 1 round (the round is charged to the sibling finding on the same line)

**Proposed text:** "And a base measured in an EARLIER run is not a base: three runs against one unchanged
prompt produced three different baselines, each stable within its own run, so an A/B figure quoted against
a base from another run is unsettled however many repeats stood behind it. Measure the base in the same run
as the arm."

**Open question for the refuter, stated rather than argued:** the two citations are different mechanisms
(cache reuse within a run; base drift across runs) under one heading. If that makes this one sighting each
of two things rather than two of one, it parks.

---

## P4 · PARK at count 1 — a text guard's WINDOW and its two halves' normalisation
`2026-08-27-chartsearchai-315.md` records five relocations that each defeated the same guard at a cycle or
round apiece: `[harden c1]` the slice ran 125 lines past the constant (fail-open, blocking-equivalent);
`[c2]` the name in a comment INSIDE the initializer; `[c3]` the copy split across the file's own line-wrap;
`[c3]` the split held apart by a BLOCK comment; `[pr r1]` the positive assertion read RAW text while the
negative read normalised text, so the name could sit in a comment while the copy sat in a sibling constant
declared OUTSIDE the slice (blocking). Clause 2 admits it on cost (≥5 rounds/cycles) but the corpus holds
one record, and this pass's adopted precedence parks a clause-2-only lesson.
**REOPEN ON:** a second record of a text/shape guard defeated by relocation rather than by rewriting, or
one where the two halves of a guard read differently-normalised text.

## P5 · PARK at count 1 — a test TOTAL summed off the wrong lines
Same record: "'3052 tests' (published in the PR body and several commit messages) -> real figure 1557, then
1559. A double count: per-class `Tests run:` lines summed against each module's `Results:` summary. · cost:
caught at r2". No round cost recorded, one record, and the project's own `CLAUDE.md` already carries a
report-the-total-not-the-lines rule, so a skill rule would be a second home for it.
**REOPEN ON:** a second record, or one where a published total cost a round.

## P6 · REPORT ONLY — the restart contradiction the record flags is already resolved
The record's "Where a skill blocked or contradicted this run" names `pr-harden` §6 vs `resolve-ticket` §1
on restarting a running standalone, and says "Worth reconciling". Both were reconciled in the live files on
2026-08-27 under an owner's instruction ("These are throwaway demo instances", `pr-harden`:322,
`resolve-ticket`:112, `verify-frontend-change`:40) — a change that is UNMIRRORED in the source repo and is
carried by this retro's commit. Nothing to propose; the observation is stale.

## P7 · REPORT ONLY — the second corroboration of an already-shipped rule
Same record: "resolve-ticket §8 says check `closingIssuesReferences` rather than the wording — it earned
its place twice here." That rule shipped 2026-08-27 (`3394ec3`). Recorded as its first post-ship exercise,
no edit proposed.

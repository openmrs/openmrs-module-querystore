# Retro window 2026-08-28 — proposals

Records read (4, all since LAST=2026-08-27):
- 2026-08-28-openmrs-module-chartsearchai-234.md (#234 / PR 326)
- 2026-08-28-openmrs-module-chartsearchai-236.md (#236 / PR 324)
- 2026-08-28-openmrs-module-chartsearchai-297.md (#297 / PR 325)
- 2026-08-28-openmrs-module-fhir2-FM2-700.md (FM2-700 / PR 629)

Linter: 10 files, 0 findings.

---

## P1 · harden Phase 2 — an agent's own output/transcript file is not a liveness signal

**Bar: clause 1 (two records).** Previous retro parked this as "No progress signal for parallel
Phase 2 agents: 1 record (#293)" with the stated reopen condition "a second record, or one where
the blind waiting costs a cycle" (REJECTED.md, running parked counts block).

Citations:
- `2026-08-27-openmrs-module-chartsearchai-293.md:27` — "harden:Phase 2 — the agent output files
  stay at 201 bytes until the agent finishes, so size-idleness is not a usable progress signal.
  In-turn waiting had to be blind `sleep` loops."
- `2026-08-28-openmrs-module-fhir2-FM2-700.md:41-44` — "harden:Phase 2 — `isolation: \"worktree\"`
  agents produced 156-byte transcripts that never grew, and I read that as a stall and killed two
  mid-investigation. Their kill notices showed both were working. Transcript size/mtime is NOT a
  liveness signal for these agents; the worktree's own `target/` mtime is. · cost: ~20 min and two
  discarded agents"

**Edit** — new sub-bullet in Phase 2 step 1, after the "If you isolate, do not assume the worktree
is on the branch under work" bullet (harden/SKILL.md:113-120):

> - **An agent's own output or transcript file is not a liveness signal.** Both runs that report it
>   describe a file that sits at its initial size until the agent finishes — 201 bytes on the #293
>   run, 156 on the FM2-700 one — so an idle size or mtime is not evidence the agent has stopped. On
>   FM2-700 it was read as a stall and two agents were killed mid-investigation; their kill notices
>   showed both working, at a cost of two discarded agents and about twenty minutes. The harness
>   reports a subagent's completion on its own, so wait for that rather than inferring death from a
>   file; where you need progress in the meantime, watch something the agent's WORK touches — that
>   run used the worktree's own `target/` — never the agent's own output.

**Prune (Step 4):** subsumes and retires nothing. Net +8 lines. Justification for the growth: the
recorded cost is destroyed live work, not friction, and the previous retro set the reopen bar at
exactly the evidence now in hand.

---

## P2 · `gate-state` usage block — mark `reviewed-sha` and `declined` as pr-scoped

**Bar: clause 1 (two records).** Previously parked as "`gate-state reviewed-sha` / `declined` reject
`--only`: 1 record (#293)", with candidate remedy "one line in the usage block saying they are
pr-scoped."

Citations:
- `2026-08-27-openmrs-module-chartsearchai-293.md:28` — "gate-state — `reviewed-sha` rejects
  `--only pr` although `await`/`clear-await` accept it; the flag is not uniform across subcommands."
- `2026-08-28-openmrs-module-chartsearchai-297.md` (Where a skill blocked) — "`gate-state ...
  reviewed-sha <sha> --only pr` rejects `--only pr` (\"unrecognized arguments\"); the subcommand
  works without it. Minor CLI inconsistency with `await`/`clear-await`, which do accept it."

Recorded cost: ~0 in both.

**Edit** — `~/.claude/pipeline/gate-state`, usage block lines 36-37, appending a comment to two
existing lines (no new lines):

>       gate-state declined --round 1 --id r1-2 --finding "…" --reason "…"   # writes the pr entry; no --only
>       gate-state reviewed-sha <sha>                                        # writes the pr entry; no --only

**Prune (Step 4):** nothing retired. Net 0 lines.

---

## P3 · skill-retro Step 6 — the hooks `cmp` clause is unsatisfiable as written

**Bar: clause 3 (a fact about the document), verified this session.**

skill-retro/SKILL.md:127 reads "**and `cmp` `~/.claude/hooks/` against the repo's `.claude/hooks/`,
every file, whether or not a skill changed.**" Checked now: `.claude/hooks/` in
`openmrs-module-querystore` holds `README.md`, `git-restore-backup.sh`, `harden-cycle-gate.sh`,
`pr-harden-gate.sh`; `~/.claude/hooks/` holds the three `.sh` files and no `README.md`. The three
scripts are byte-identical. Read literally, "every file" fails on a README that documents the
directory and is not meant to be installed, so the check reports a difference every retro forever,
or the reader installs a README as a hook.

**Edit** — one phrase in skill-retro/SKILL.md:127:

> "…every file…" → "…every `.sh` file…"

`git-restore-backup.sh` — the file that clause was added for — is a `.sh`, so the narrowing keeps
the coverage the bullet exists for.

**Prune (Step 4):** nothing retired. Net 0 lines.

---

## P4 · skill-retro Step 6 — the sync list omits `.claude/pipeline/`

**Bar: clause 3 (a fact about the document).** No observed drift: checked this session,
`~/.claude/pipeline/{gate-state,pool-run,pool-watch,pool-test.py}` are byte-identical to the repo's
`.claude/pipeline/` copies. The gap is in the obligation, not in the current state, and this
proposal states the guard rather than a diagnosis.

Step 6 mandates `cmp` for the skills, for each gate's live-hook/live-skill pair, for
`~/.claude/hooks/`, and for `~/.claude/skill-lessons/`. It does not mention `~/.claude/pipeline/`,
which holds `gate-state` — the helper `harden/SKILL.md:227` and pr-harden's State section instruct
every run to call, and which P2 above would edit. That is the same class the bullet's own note
names: "the first draft of this step's wording created a fourth copy with no sync obligation, which
is the drift class the whole bullet opens by naming" (skill-retro/SKILL.md:126-127).

**Edit** — extend the existing hooks bullet's scope rather than adding a bullet:

> "…and `cmp` `~/.claude/hooks/` against the repo's `.claude/hooks/`, every `.sh` file, whether or
> not a skill changed." → "…and `cmp` `~/.claude/hooks/` and `~/.claude/pipeline/` against the
> repo's `.claude/hooks/` and `.claude/pipeline/`, every `.sh` file and every helper a skill
> invokes by path, whether or not a skill changed."

**Prune (Step 4):** nothing retired. Net 0 lines (rewords one existing sentence).

---

## Observed, not yet actionable (parked, with counts)

- **`git checkout -- <path>` losing uncommitted work: 11 incidents / 9 records** (#302, #284, #268,
  #269 ×2, #250 ×2, #308, #317, +#234, +FM2-700). Remedies stay killed. Neither new incident meets
  the standing reopen condition (an incident where "commit before probing" WAS followed) — both are
  probes on files carrying uncommitted work. FM2-700 reports that a `cp` aside worked the second
  time; that is the remedy killed on four blocking objections at the 2026-08-24 retro (REJECTED.md
  P1, objection 1: a `cp` taken at mutation time reverts a concurrent edit exactly as a remembered
  copy does), so it is not new evidence.
- **Prose-correction cycles: 11 records.** +FM2-700 ("SIX separate false sentences, one found per
  confirming pass, five of them written while correcting the previous one · cost: 5 harden
  cycles"), +#297 ("cycles 2-4 were all corrections to prose written the cycle before … The escape
  was the skill's own 'delete rather than reword' counsel, and cycle 4's only edit was a deletion"),
  +#236 (a round-3 correction "made a vague-but-true sentence sharper and false"). Remedy killed on
  the walk-forward (REJECTED.md 2026-08-24 P3); deletion-over-rewording is the shipped rule and is
  what converged #297.
- **`gh issue view` returns EMPTY (exit 0) where `gh api repos/<o>/<r>/issues/<n>` works: 1 record**
  (#236, "Every agent brief had to carry the workaround"). No round or cycle cost recorded.
- **A stale jar in `.openmrs-lib-cache/<module>/lib/` shadowing a freshly deployed fix: 1 record**
  (FM2-700, cost 1 restart).
- **`mvn install` re-dirtying the tree between a spotless revert and the commit, so `git add -A`
  commits unrelated churn: 1 record** (FM2-700, cost 1 amend).
- **An agent killing a co-tenant's standalone by misattributing PIDs before checking cwd: 1 record**
  (#297; the brief already said to check cwd and the agent did so only after the first kill).
- **A python slice replacement whose `end` anchor had moved above `start`, silently duplicating a
  block: 1 record** (#234, caught by the compiler, cost 0). The record notes harden:300's "count
  what should still be there" is aimed at deletions.
- **Inserting a constant between a javadoc and the member it documents, orphaning the javadoc: 1
  record, 2 incidents in one slice, both silent** (#234 h-c2).
- **A published count that does not reproduce from its own stated predicate, or that is a property
  of something other than what it names: 1 record, ≥3 Phase 2 passes** (#236). Candidate remedy is
  plausibly already reached by harden:299 ("Don't publish a claim a later cycle must re-measure")
  and by the chartsearchai CLAUDE.md measurement rule.
- **harden's confirming-cycle cost vs where the blocking findings come from: 1 record** (#236 — 5
  Phase 2 passes over a 15-line change found no blocking item; both blocking-grade guard bypasses
  came from pr-harden's clean-context rounds).

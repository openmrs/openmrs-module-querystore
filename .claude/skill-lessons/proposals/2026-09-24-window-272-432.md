# Proposals — 2026-09-24, tenth window (records #272/PR534, #432/PR536)

## P1 — resolve-ticket Step 3: a gate objection that NARROWS coverage is settled by the mutation it exempts

Bar (a): #407:8/:18 (parked 2026-09-24 seventh window, "REOPEN ON: a second record") and #432:9/:14.
#432 alone also meets bar (b): one harden cycle.

Insert in `resolve-ticket` Step 3, after the paragraph *Check it, do not estimate it — the objection's
own numbers included* (ending "...means changing a signature and building."):

> **An objection that NARROWS what the planned test or guard covers is the same kind of claim** — an
> exclusion, or "one case pins it" — and the mutation it exempts is what settles it: once the code
> exists, run that mutation before `/harden`. On #432 gate pass 1 made blocking an exclusion copied
> from a shared helper, and defaulting the constructor rung it hid left the whole api suite green,
> found by `/harden` a cycle later; on #407 the refuter's "one case pins it" left two per-conjunct
> gates green, found by round 1.

Prunes: closes the seventh window's 1-record park (*Refuter's scope narrowing overturned by a
reviewer's per-conjunct measurement*). No existing clause subsumed; net +6 lines.

## Refuter's reply

(appended below)

Verdict: **KILL** (fresh general-purpose agent `a85eb00dab7906a0e`, read-only; citations re-checked by
the retro against the files).

- Subsumed: `harden/SKILL.md:272-278` ("an exemption you WRITE into a guard ... a by-name exempt file
  ... So build the case the exemption ADMITS, and watch it pass"), mirrored in `pr-harden`. #432's
  catch was harden c1 P2 (#432:13), i.e. that rule firing in its home. A second copy in Step 3 is the
  "rule exists, more prose" class (REJECTED.md:3775-3776).
- Bar (b) misapplied: `skill-retro/SKILL.md:75` needs two or more; #432:8 records one harden cycle.
- Citation wrong: the draft cited #432:9/:14; the exclusion is at :8 and :13.
- Class stretched: #407 is a reasoned sufficiency cut (#407:18), #432 a correct false-positive
  objection whose adopted remedy was too coarse (transcript jsonl :132, :135).
- Would not have prevented #432: a whole-class exclusion exempts every mutation in it, and the draft's
  singular "the mutation it exempts" names none.

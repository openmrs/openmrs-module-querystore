# Retro window: 2 new records — #266 / PR 322 and #293 / PR 323 (both 2026-08-27)

Linter: 10 files, 0 findings.
Records read: `2026-08-27-openmrs-module-chartsearchai-266.md`, `2026-08-27-openmrs-module-chartsearchai-293.md`.
(`2026-08-27-retro-authored-hook-regression.md` was processed by the previous retro, commit `9866a1f`.)

---

## P1 · pr-harden step 6.2 + verify-frontend-change:29 — the JDK rule names one direction and hardcodes 1.8

**Bar: clause 3 (a skill contradicting itself), plus one record.** `pr-harden`:317-321 says "Build under
the JDK the pom targets" and then, in the same sentence, gives the remedy as
`/usr/libexec/java_home -v 1.8`. The module this pipeline actually runs on declares
`maven.compiler.target` = **11** (`openmrs-module-chartsearchai/pom.xml:34`), so the illustration
contradicts the rule it illustrates. `verify-frontend-change`:29 is a second home of the same pair.

**Record:** #266, "Where a skill blocked or contradicted this run" — "pr-harden:step 6 — the verifier's
procedure anticipates a Java-21-vs-1.8 mismatch and suggests `java_home -v 1.8`; the actual failure was
the OPPOSITE (`invalid target release: 11` under JDK 8, machine default 21). Cost: one repair attempt."

**Edit:** replace the remedy with the pom's own target and name both signatures — too new a JDK for an
old target gives the `MockitoException … Java: 21` wall; too old a JDK gives `invalid target release: <n>`
at compile. Prunes the hardcoded `1.8` in both files (it is the false half).

---

## P2 · pr-harden "Correcting a claim means finding every home of it" + harden anti-pattern:291 — grep the rarest TOKEN, not the phrasing

**Bar: clause 1 (2 records) and clause 2 in each of them.**

- #266: "the two-format claim about the groups file has N homes -> seven, found one per cycle, each
  hidden by a NEW mechanism: a data file rather than a doc; markdown emphasis splitting the phrase; a
  line break between quantifier and noun with wording matching no other home · cost: 3 harden cycles."
- #293: "'reversing foldedPartnerLabel's branches puts a bare ATC code in BOTH sentences' (a 2026-08
  measurement in five texts) … cost: 2 cycles (found in one, a fifth home found in another)"; and
  "Asserted in four places (javadoc, CLAUDE.md, ADR 40, a test) before a Phase 2 agent measured it."

**Why the existing rule did not prevent it:** both homes of that rule say to grep for the claim's
*distinctive phrasing*. Every survivor #266 paid for was a home a phrase grep structurally cannot hit —
the phrase was split by the file's own formatting, or the home was not prose.

**Edit:** change the method from "the claim's distinctive phrasing" to **its rarest single token, over
the whole tree including data and resource files**, and name the three mechanisms. Replaces the method
sentence rather than adding beside it.

---

## P3 · same two sections — a duplicated DEFINITION is a home too, and the suite stayed green in all three

**Bar: clause 1 (3 records).**

- #315 `[r6]` (parked ledger, count 1): "A fix applied to one member of a script family, its sibling
  left fail-open."
- #266 ×2: "`configuredDataFileNotRead`'s detail said 'The entry count …' for the GROUPS dataset — the
  exact defect the items parameter was added to prevent, applied to one rule and not its sibling ·
  cost: 1 cycle"; "The item-noun assertion covered the curated call site only; crossing the DDInter
  literal survived the whole suite · cost: 1 cycle."
- #293: "the whitespace normal form was defined TWICE (two independent `Pattern.compile("\\s+")`);
  widening one and not the other reddened NOTHING … while making a name unfindable in the record that
  renders it."

The parked entry's own objection was that the every-home section "is about CLAIMS; this is code". That
is the gap: three records, and in each of them the un-widened sibling left the suite green.

**Edit:** one sentence in each home — before applying a fix to a definition, grep for a second
definition of the same thing; a green suite after fixing one member is not evidence the family has one
member.

---

## Parked, not proposed

- **Cross-session interference on a live run's shared state: 2 records, two different remedies.**
  #266's own appended correction (a concurrent session ran `gate-state --cwd <path> clear` against this
  run's worktree and released its slot lease, deleting the checkout twice mid-verifier) and
  `2026-08-27-retro-authored-hook-regression.md`'s "candidate rule 4, not proposed" (a concurrent
  session's edit absorbed into a commit and described in this session's voice; the `cmp` checks
  compare live-against-repo after the copy and cannot see that the live file moved). The class is
  corroborated; the remedies are not the same remedy, so neither is at 2. **Refuter question: are these
  one event described twice?** Different days' work and different mechanisms, but both name an
  unidentified "other session".
- **`gate-state reviewed-sha` / `declined` reject `--only`** (#293). 1 record, ~0 cost, and the script is
  right by construction: both write only the `pr` entry. Candidate remedy is one line in the usage block
  saying so. Not clause 3 — the skill documents `--only` for `await`/`clear-await` only.
- **No progress signal for parallel Phase 2 agents** (#293: "the agent output files stay at 201 bytes
  until the agent finishes, so size-idleness is not a usable progress signal. In-turn waiting had to be
  blind `sleep` loops"). 1 record. `harden` prescribes no size-idleness, so this is a gap, not a
  contradiction.
- **A mutation surviving because the only covering case is in a degenerate state** (#266: hardcoding
  four of the crossReactivity map's five keys left the suite green, because the only case reading it
  drives the DISABLED state where all five equal the mutation). 1 record, 1 cycle. Arguably already
  reached by harden's "ask of any clean or zero result what its inputs could not have produced".
- **harden Termination vs. the claims-about-claims anti-pattern** (#293: "The skill names that signature
  and tells you to change tactics, but its termination rule still demands a cycle that changes nothing.
  The two pull against each other; took the labelled override"). Proposed as a contradiction by the
  record and NOT carried here: :288's tactic is *delete*, and a deleted clause generates no successor
  claim, so the recursion terminates — this reads as the shipped rule being violated rather than two
  rules conflicting. Parked-count class "Prose-correction cycles" instead.

## Explicitly no change

#266 records "`gate-state pr-set` dropped `declined` and `reviewed_shas`" and its own appended
correction retracts it: `pr-set` uses `setdefault` for both and cannot drop them; a foreign
`gate-state clear` removed them. The skill's claim stands. Verified in `~/.claude/pipeline/gate-state`
(lines 209, 250-256).

---

## Outcome (Step 5 refuter verdicts, applied)

- **P1 — KILLED.** Not clause 3: the sentence states its own antecedent ("a module on Java 1.8"), so the
  remedy agrees with its premise; the pom is a fact about the world, and skill-retro:66-68 restricts
  clause 3 to facts about the document. Left at 1 record, cost "one repair attempt", below clause 2.
  Its "1.8 is the false half" was itself false. Banked with a reopening condition.
- **P2 — APPLIED, revised.** Clause 2 on #266 alone; the #293 corroboration did not hold (an ellipsis
  had reframed :11, and :10 is harden:289's class). Shipped without the "Every … structurally cannot"
  universal and without a closed list of mechanisms; the existing after-check kept in both homes.
- **P3 — APPLIED, revised.** 3 records, not 4 sightings. Noun widened from "a definition" to the family
  the records share, because as submitted it reached one of its three sightings. One home (harden:192),
  not two. Opens with what it adds over the revert-check it sits beside.
- **P4 — KILLED.** The quotation merged two bullets: the glob is on the live↔repo mirror obligation
  (:116-120), while the hook-copy obligation (:121-126) says "each gate" and defines it by the install
  line. The narrower wording would also drop both `gate-test.sh` harnesses from the repo mirror, and
  those have drifted on the record. 0 records / 1 measurement.
- Parked classes and their counts are in `REJECTED.md`'s 2026-08-27 (third window) block, which also
  supersedes the hook-regression record's own "Count so far: 1 record" for the cross-session class.

# Retro window 2026-09-14 (second pass of the day) — records #421, #337, #276

Records read: 3, all written after the 01:22 retro whose window was #294/#409/#413.

- `2026-09-14-openmrs-module-chartsearchai-421.md` — resolve-ticket + harden + pr-harden, #421/PR423, converged, 2 rounds / 6 cycles
- `2026-09-14-openmrs-module-chartsearchai-337.md` — resolve-ticket + pr-harden, #337/PR422, converged, 2 rounds / 3 cycles
- `2026-09-14-chartsearchai-276.md` — resolve-ticket + pr-harden, #276/PR424, pr-harden converged; harden did-not-converge, override after cycle 7

Linter: `skill-lint.py` over `~/.claude/skills` — 10 files, 0 findings.

---

## P0 — PRUNE + CORRECTION. `pr-harden` **State**: delete the paragraph claiming the unattended marker decides entry ownership.

**The edit.** Delete `pr-harden/SKILL.md` lines 918-928 ("**And that marker now decides OWNERSHIP as
well as attendedness…**"). Fold its one non-duplicated clause — an indeterminate ancestor walk keeps
the block — into the `owner` paragraph at 861-871, which is where the gate's reasoning already lives.

**Corroboration: Step 3 clause 3 — the skill contradicts itself AND its own gate script.** Single
instance suffices, because it is a fact about the document.

- Line 918 asserts the marker "now decides OWNERSHIP as well as attendedness".
- Line 861, 46 lines above, asserts the opposite: "**`owner` is what tells your entry from somebody
  else's, and it is not the unattended marker's job.**"
- Lines 872-879 then record the measured cost of the reading line 918 states: "**Do not answer this
  question with the unattended marker.** The first version of that check inferred entry ownership from
  marker ownership, and review measured the cost within the hour: a live foreign marker allowed EVERY
  block path… `edits: 7` allowed, `phase: fixing` allowed." So line 918 re-proposes a regression the
  same file pins in `gate-test.sh`.
- The gate settles it. `~/.claude/hooks/pr-harden-gate.sh` header: "ASK IT OF THE ENTRY, NEVER OF THE
  UNATTENDED MARKER." The code reads `.owner` off the ENTRY for ownership (`OWNER_PID=$(jq -r
  '.owner // empty' …)`, then `kill -0` + `owns_this_session`); the marker block builds
  `$HOME/.claude/pipeline/unattended/<key>.json` and its `owns_this_session` call decides whether
  THIS session is unattended. Two questions, two files.
- The rest of the deleted paragraph is verbatim restatement: the 2026-08-26 incident and both
  damaging remedies are already at 863-867; "the state file is still keyed on `$PWD` alone, so two
  runs in one checkout share one entry and the later writer wins" is already at 878-879.

**Prunes:** ~11 lines, one of them false. This is what pays for P1-P4.

---

## P1 — `harden` Termination: a documentation cycle whose findings are the previous one's own prose.

**The edit.** After "A documentation pass that turns up anything behavioural escalates on the spot…"
(harden 215-217), add:

> **And a documentation cycle whose findings are all claims the PREVIOUS documentation cycle wrote is
> not fixed by another cycle** — correcting them is what generates them. #276 spent five consecutive
> such cycles and took the override after seven, with the code converged at cycle 1; #337 spent four
> passes the same way. Reach for the CLAIM SHAPE clause of *Don't rewrite prose faster than you verify
> it* on the SECOND such cycle, not the fifth.

**Corroboration: two records, and ≥2 cycles in each.**

- #276, *Where a skill blocked or contradicted this run*: "harden:Termination — the cycle gate is
  edit-based, and five consecutive cycles found only documentation counts, each introduced by the
  previous cycle's own documentation edit. The rule kept the run going long after the CODE converged
  (cycle 1). Took the labelled override after cycle 7. What broke the loop was not another cycle but
  changing the fix from correcting counts to REMOVING them."
- #337, *Raised by a fresh agent*: "[harden P2/P3/cycle2/cycle3] Six separate passes each found one
  more false or stale claim in prose the previous pass had written — counts of marker spellings, 'the
  ONE way', 'all three', 'three of four', singular 'the ASCII spelling' · cost: 4 harden passes".

**Why a pointer rather than a rule.** The rule already exists, twice (harden 383-388). What the two
records show is that it was not REACHED: both runs name Termination as the section they were fighting,
and the rules live 150 lines away in Anti-patterns. This adds the trigger condition at the place the
run is standing, and nothing else.

**Prunes:** nothing. +4 lines.

---

## P2 — `harden`: "a change in the KIND of question" is not about text guards alone.

**The edit.** At harden 268, after "**What ends the loop is a change in the KIND of question, not
another entry on the list.**", add one sentence:

> Not of text guards alone: on #421 a predicate over TYPES took four cycles of successive widenings —
> exact equality, then `Drug[]`/`List<Drug>`, then a raw type, then wildcard and type-variable bounds
> — each escape found by the next fresh agent, and the enumeration that replaced it was wrong in both
> directions; on #276 a production text predicate was rewritten four times, each wording refuted by a
> different reviewer with a real input, and it settled only when the question became whether the stop
> BEGINS a token rather than which characters to refuse.

And at harden 396, drop the scoping clause: "requires of a guard" → "requires".

**Corroboration: two records, four cycles/passes each.**

- #421: harden c2 "The predicate used exact type equality, so Drug[] and List<Drug> on the carrier
  were handed straight to the loop · cost: 1 cycle"; c3 "A parameterized type's RAW type went unread,
  so a generic subclass of Drug escaped · cost: 1 cycle"; c4 "Wildcard and type-variable bounds went
  unwalked; List<? extends Drug> escaped · cost: 1 cycle"; c4 "The 'two questions' that replaced the
  enumeration were wrong in BOTH directions (List<Serializable> answers no to both and is reported) ·
  cost: 1 cycle".
- #276: "The needle rule, four successive wordings, each refuted by a different reviewer with a real
  input: refuse-any-separator lost a list comma; refuse-between-two-digits lost a naked decimal;
  refuse-unless-a-letter-precedes lost a comma after `)`; the same for the full stop lost every other
  closing mark. Ended only when the question changed KIND (does the stop BEGIN a token) instead of
  enumerating characters. · cost: 4 passes".

**The gap being closed.** The sentence sits inside a bullet opening "**And where the guard is over
TEXT**", and harden 396 cites it as something the rule "requires of a guard". #421's cascade was
neither over text nor a test guard — it was a production predicate over TYPES — so on the shipped
wording the rule did not reach the case that cost four cycles.

**Prunes:** one scoping clause. +3 lines net.

---

## P3 — `harden`: the write-side cause of an orphaned javadoc, not only the detector.

**The edit.** In the insertion clause at harden 389, after "read the neighbours of the insertion point
and check that each doc comment still sits against the member it names", add:

> **Anchor the insertion after the preceding member's closing brace, never on the following member's
> signature** — that is what does it. It recurred twice in one run after the three above (#276), the
> second time stranding the canonical statement of a rule three `{@link}`s point at.

**Corroboration: the defect appears in four records.** Already cited in the skill: #234, #229, #255
("silent through compile, checkstyle and 1686 tests; on #255 three agents reported it independently").
New: #276, *Where a skill blocked or contradicted this run* — "Twice in one run I orphaned a javadoc by
inserting a member above an existing one — the failure my own memory entry records. The second time it
stranded the canonical statement of a rule three `{@link}`s point at. Anchoring on a signature rather
than after a closing brace is what does it."

**Why it is not already covered.** The whole bullet, including the `awk` one-liner added at the 01:22
retro, DETECTS an orphan after the fact. #276 is the first record to name the write-side cause, and a
prescription at the moment of the edit is cheaper than a sweep after it.

**Prunes:** nothing. +3 lines.

---

## P4 — `harden` Termination: both error directions of a check that REPORTS.

**The edit.** A new bullet in the Termination list, after the "ADDS a guard or clause" obligations
(harden 235):

> - **Where the thing you changed REPORTS — a warning, a finding, a check — measure both error
>   directions, because a fix that closes a miss moves the other one too.** Two runs, each caught by a
>   fresh agent rather than by the author: on #337 a carve-out applied to both operands withdrew the
>   record-side exit, so an answer reproducing an operator note FAITHFULLY was reported — the
>   crying-wolf failure the check exists not to have — and the carve-out had to become per-operand; on
>   #276 a boundary said to "fail toward silence" matched a fragment of a larger number and produced a
>   false report on a published key. Name the direction the check must never fail in, and build the
>   input for it.

**Corroboration: two records, and ≥2 passes in one.**

- #337, *Raised by a fresh agent*: "[harden P1] Carve-out applied to RECORDS too withdrew the
  record-sentence exit at the `endSentence` seam, so an answer reproducing an operator note ending in
  an ellipsis FAITHFULLY was reported — the crying-wolf failure the check must not have · blocking".
  And *Assumptions review overturned*: "'The ellipsis slice is threshold-free' -> true only for the
  answer operand; on the record side it trades a miss for a false report and the carve-out had to
  become a per-operand parameter".
- #276, *Refuted by measurement*: "'statesWord's boundary fails toward silence in the direction that
  matters' -> `statesWord("…2.5 mg/day…", "5 mg/day")` is TRUE; a decimal point is neither letter nor
  digit, so a ceiling matched a fragment of a larger number and produced a FALSE REPORT on a published
  key. · cost: 1 pass, and 4 more wordings after it". And "[harden p2] The decimal false POSITIVE
  (above) — the direction the check must never fail in."

**Overlap to test.** `pr-harden` 293-306 asks "which case hands the guard's SUBJECT its other value",
and harden 242-250 requires a positive control for a guard that should stay green. Both are about the
guard's own boolean being exercised. P4 is about the FIX moving the check's other error direction on
input the suite already had — in #337 the false report was on a faithful reproduction, in #276 on a
published key. If the refuter judges this subsumed, drop it.

**Prunes:** nothing. +4 lines.

---

## Net line budget

Added: ~14. Pruned: ~11 (P0), one of which was a false claim contradicting the gate. Net ~+3.
Justification for the growth: P0 removes a claim that re-proposes a pinned regression, so the prune is
worth more than its line count; and three of the four additions are trigger conditions or pointers
placed where the run is standing, not new arguments.

---

## Observed, not yet actionable (parked, with counts)

1. **A test pinned from one side only survives a TIGHTENING mutation.** harden 235 lists "deleted, its
   arms swapped, its comparison **loosened**, or rewritten in a semantically equivalent way". #337's
   defeat was `>=` → `==`, a tightening: "[r1] The dots-run test was pinned from BELOW only: `>=`->`==`
   (four-dot cut) and an added end-of-gap test (unspaced cut) each reinstated the defect with all 2140
   tests green · blocking · cost: 1 round". **Count: 1 record, 1 round.** Below the bar on both clauses.
2. **A third mechanism by which a mutation loop silently runs nothing.** #421: "macOS has no
   `timeout(1)`; a mutation loop using it silently produced no output and no error (the `||` fallback
   never fired because grep exited 0 on empty input). Cost: one confused re-run." The FAMILY is already
   ruled (harden 236-241, #256 and #263), and that same rule's neighbour says what ends such a loop is
   a change in the KIND of question, not another entry on the list — so this is recorded as the third
   instance and deliberately not added. **Count: 3 records for the family, 1 for this mechanism.**
3. **A logger positive control before reporting an absent WARN.** #421: the verifier "Established a
   LOGGER POSITIVE CONTROL before reporting absent warnings, unprompted by the brief's wording —
   without it an absent WARN proves nothing." The brief does not ask for it. **Count: 1 record, cost 0**
   (the agent did it anyway).

## Corroborated as WORKING — recorded so a later retro does not "fix" them

- **The dead-phase contract survives a session usage limit.** #421: "Session usage limit killed all four
  cycle-3 Phase 2 agents mid-flight; pool-run re-invoked after reset and the run resumed from state on
  disk with nothing lost." #337: "one session usage-limit stop mid-run, resumed from disk with nothing
  lost." 2 records.
- **All three of `pr-harden` Step 1's base-moved hazards, fired at once.** #276's addendum: the ADR
  decision-number collision (main took Decision 95 too → renumbered to 96, swept BY NUMBER), the shared
  instruction-file budget (main spent 139 bytes of the headroom this branch had trimmed to fit; merged
  CLAUDE.md came out 126 over the cap, trimmed own prose rather than raising the number), the ADR's own
  INDEX missing both decisions, and "the clean merges were where the risk was — three files auto-merged;
  all of this branch's claims about them had to be RE-MEASURED, not re-read. All held." Every one of
  those is the documented remedy being executed. The index class is now at its fifth instance
  (#280/PR383, #348/PR369 ×2, main's own, #276).
- **`pr-harden` FINISH's "do not edit the cleared sha".** #276: "sent two real non-blocking findings to
  a follow-up issue rather than into the branch. Correct, and worth noting: the branch ships with a
  known named residue."
- **harden's cycle gate catching a phase verdict mistaken for a run verdict.** #337: "the phase gates
  are finding-based, so three Phase-2 passes each 'converged' while the cycle had edits in it; the cycle
  gate is what forced cycles 2 and 3, and cycle 3 (single-agent documentation confirm) is the one that
  found nothing" — the documentation-pass classification working as designed.
- **`resolve-ticket` Step 9 / Stop gate.** #421: "the run completed harden and reported without opening
  the PR; the gate caught it and named the owed phases. Correct catch, cost ~0."

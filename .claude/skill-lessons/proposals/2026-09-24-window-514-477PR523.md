# Proposals — 2026-09-24, fifth window: #514/PR524, #477/PR523

## P1 — a run record is appended with `>>`, and the driver reads the LAST run's outcome in it

Bar (a): 2 records of one run's record destroyed by a later run on the same ticket, same UTC date.
- #488, 2026-09-23: the PR #501 run wrote `…-488.md` over the PR #490 run's record
  (REJECTED.md :3909-3912, restored from `846a225`). Transcripts `39ed9650`:769 and `d52a1da1`:531
  both `cat > ~/.claude/skill-lessons/2026-09-23-openmrs-module-chartsearchai-488.md <<'EOF'`.
- #477, 2026-09-24: the PR #523 run (`608ffbd8…`:785) did `cat > …-477.md <<'EOF'` over the PR #509
  run's record, pushed at `dcafb6c`. Found at this window's Step 6 pre-read comparison; the PR #509
  record restored from `origin/main`, the PR #523 record kept beside it as `…-477-PR523.md`.

Both skills say "Append a record to `…/<UTC-date>-<repo>-<ticket-or-pr>.md`"
(resolve-ticket:600, pr-harden:1196). Both runs replaced. Cost in each: an evidence record silently
lost from the live store; recovered only because the first was already pushed.

Why not a different NAME for the second run: `pool-run` `record_written` (:2830) attributes a record by
`re.search(rf"[-_]{num}(-driver)?$", stem)`, so `…-477-PR523.md` or any suffix after the ticket number is
invisible to it as THIS ticket's record, and falls to the unnumbered fallback.

Edits:
1. resolve-ticket and pr-harden, "Write the run record", after the first sentence:
   > **Append means `>>`, never `>` or the Write tool.** The name is keyed on date and ticket, so a second
   > run on one ticket the same day lands on the first run's record: `cat >` replaced it on #488
   > (2026-09-23) and #477 (2026-09-24). Keep the name — it is how `pool-run` finds your record.
2. skill-retro Step 1: "one per finished run" -> "one per finished run, or several where a later run on
   the same ticket and date appended to it, each opening with its own `# ` header".
3. pool-run `record_says_aborted` (:3100): read the first 12 lines of the LAST `# `-headed record in the
   file, not the file's first 12. Otherwise a second run that aborts, appended after a first run that
   converged, reads as a crash and is retried — the direction the docstring says this function may never
   push ("it can only make the driver do LESS"). `DRIVER_HEADER` is `## …`, so a driver capture appended
   after the run's own record does not start a new chunk.
   pool-test.py: a two-run file, first converged second aborted -> True; first aborted second converged
   -> False; a single aborted record with a driver capture appended -> True.

Prunes: nothing retired — it makes "Append" concrete where two runs read it as "write". Net: +3 lines
each in resolve-ticket and pr-harden, 0 in skill-retro (a clause), plus the driver's test.

## Not proposed, parked

- harden Phase 2 escalation chain: +#514 (six Phase 2 rounds, five escalated), +#477-PR523 (one
  escalation) -> 7 records. #514 names no remedy that keeps each pass's real defect. REOPEN unchanged.
- pr-harden rounds whose blocking finding the PREVIOUS round's fix introduced: #514 r2->r3->r4, a
  free-prose recogniser widened per finding; cost 3 rounds and non-convergence. Bar (b) met, no remedy:
  the round cap stopped it and left the PR draft as designed. Same family as the escalation park.
- #514 "ADR residue list 'these N legs are unpinned'" cost 2 harden cycles, stopped at deletion: an
  instance of harden:489 ("Don't publish a claim a later cycle must re-measure"), followed late. No edit.
- Prose the change made false, found by a fresh agent: +#514, +#477-PR523 -> 7 records. REOPEN unchanged.
- chartsearchai nested CLAUDE.md byte budget: +#477-PR523 (7 bytes left; r1-5-style pointer declined)
  -> 6 records. REOPEN unchanged.
- main moving under a run: +#514 (ADR Decision 116 taken by #523, renumbered 117 at rebase, 0 cost) ->
  3 records. REOPEN unchanged (none cost a round).
- 1 record: `gate-state` has no remove subcommand; an entry written with placeholder values was removed
  by hand under the lock (#514, no harm).

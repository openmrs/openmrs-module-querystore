# Retro proposals — window 2026-09-23 18:00 → 21:09 (7 records: #479/PR486, #480/PR484, #482/PR487, #485/PR493, #488/PR490, #489/PR492, #491/PR495)

Status: refuted (one round; 0 killed, 3 revised — see REJECTED.md 2026-09-23 second window). Line citations in this draft are off by 1-3; the ledger entry carries the corrected ones.

## P1 — the prescribed `gh issue view … --comments` never prints the issue body when stdout is not a TTY

Bar: (a) 4 records this window (#482:23, #485:21, #488:22, #491:21, each "`gh issue view` empty at exit 0
again; gh api fallback worked") on top of the 3 the warning cites (#236, #255, #347); and (c) the skills
prescribe the command and then diagnose its output as a `gh` failure, which a measurement contradicts.

Measured 2026-09-23, gh 2.87.3, from a tool shell (non-TTY), with a control of each kind:
- `gh issue view 491 --comments` → exit 0, **0 bytes**. #491 has 0 comments (`gh api …/issues/491/comments --jq length` → 0).
- `gh issue view 480 --comments` → exit 0, **655 bytes: the one comment only**. The body's first 40 chars
  (`gh api …/issues/480 --jq .body`) occur 0 times in that output. #480 has 1 comment.
- `gh issue view 480` (no `--comments`) → 3053 bytes (body present).
- `gh issue view 480 --json title,body,comments` → title, body length 2802, comments 1: both in one call.

So "empty at exit 0" is not a `gh` failure: it is `--comments` on an issue with no comments. The worse
case is silent. On an issue WITH comments the prescribed command exits 0 with non-empty output and no
body, so the current warning ("empty output … is that failure") cannot fire, and the ticket is read as
its comments alone.

Edit:
- `resolve-ticket` Step 1 table, both GitHub rows: `gh issue view 123 [--repo <owner>/<repo>] --json title,body,comments`.
- Replace the paragraph "**Empty output at exit 0 from `gh issue view` is that failure…** … before briefing
  anyone." with a paragraph stating the measured mechanism (non-TTY `--comments` prints the comments and
  never the body) and that `--json title,body,comments` returns both.
- `pr-harden` :225 and `pr-review` :16: same invocation, and the pr-harden clause pointing at resolve-ticket kept as a pointer.
Prunes: the three-run history paragraph (its diagnosis is what the measurement retires; the records stay
in REJECTED.md). Net ≈ 0 lines.
Not edited: `~/.claude/CLAUDE.md`'s `gh` bullet is the owner's own instruction file, so it is reported and not changed.

## P2 — `gate-state`'s `--only` guidance does not reach `--only` placed BEFORE the subcommand

Bar: (a) 2 records — #485:22 ("`gate-state --only pr await` fails with \"invalid choice: 'pr'\"" · one call),
#488:21 ("`--only pr` must go after the subcommand, and my first two tries failed"); and (c) `_Parser`'s
docstring says the guidance "is attached to the error itself, which is the one text the caller is certain
to read". For this shape it is not.

Measured: `gate-state --cwd /nonexistent-x --only pr await x` → `error: argument cmd: invalid choice: 'pr'`
with no guidance (the message does not contain `--only`). `… reviewed-sha abc --only pr` → the guidance fires.

Edit: in `_Parser.error`, when `--only` is in argv ahead of the subcommand, append that `--only` goes AFTER
`await` / `clear-await` / `clear`, with one example. Plus a `pool-test.py` case that runs the misplaced form
and asserts the guidance appears in stderr (red before the edit). Prunes nothing; +~6 lines of code, +1 test.

## P3 — `pull/<n>/head` lagging a push: the parked lesson's reopen condition is met

Bar: (a) 2 records — #444 (REJECTED.md 2026-09-20 window, "round 2's first fetch returned round 1's sha")
and #491:23 ("fetch of pull/495/head right after push returned the old sha … caught by the reviewed_shas
comparison, re-fetched with '+'"). REJECTED.md's parked entry: "**REOPEN ON:** a second".

`pr-harden`'s reviewed_shas paragraph lists "the push had not landed when the fetch ran" as one possible
cause and prescribes nothing for it. Both records now establish it as a cause. Neither record shows a
reviewer reviewing the stale sha: the guard caught #491, and #444 retried.

Edit (one sentence appended to that paragraph): where the fetched sha is not the sha this run just pushed,
the ref lagged the push (#444, #491). Re-fetch with a forced refspec (`+pull/<n>/head:pr-<n>-r<round>`) until
it matches, and never spawn on the lagging one. The "Two pairs … never established" paragraph stays true of
those two pairs and is kept. +2 lines.

## Parked (below the bar, or a bar met with nothing to write)

- harden Phase 2 run as 1–2 agents covering the four lenses on small diffs, the deviation stated: 3 records
  (#479, #488, #491). The bar is met, but no record measures a cost either way, so any text (licensing it or
  forbidding it) would state a claim nobody checked. Parked, awaiting a record where the merged-lens pass
  missed something a later fresh agent found in that lens, or a four-agent pass found something a merged one did not.
- Stop hook firing while waiting on background work, recovered in-turn: 2 records (#479, #489 one turn).
  `resolve-ticket`:448 already names this. The shipped rule is working, so no text.
- #482: three harden Phase 2 passes each found one more silent shape of one predicate. The KIND-of-question
  change came only in pr-harden r1 (harden:313 already carries that rule). 1 record, ≥2 cycles: bar (b) met,
  but it is a shipped rule not applied, and more prose is REJECTED.md :1196-1198's class. Parked at 1.
- measurement deliverable vs resolve-ticket Step 5's test-first framing: 1 record (#480), cost 0.
- `gate-state await` has no per-agent clear: 1 record (#480).
- base moved by a clean non-required merge after round 1, whether to push it: 1 record (#489).
- editing the branch while a read-only refuter runs: 1 record (#485), harmless.
- user CLAUDE.md names `git-restore-backup.sh` as if it were a command (it is a PreToolUse hook): 1 record (#485).
- shell decoding a `–` in a mutation script before Python: 1 record (#482), caught by the assert.

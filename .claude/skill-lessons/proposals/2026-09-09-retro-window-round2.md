# Retro window 2026-09-09 · round 2 submission (post-gate)

Gate round 1 ruled: **P2 KILL→park** (a gap is not a contradiction; `REJECTED.md`:331-333, :1103; and
#393's case does not land in the enumerated cause it was said to, because that cause's parenthetical
requires the fixer to have declined everything and `…-PR393.md`:58 records nothing declined).
**P3 KILL→park** (`pr-harden`:579-580 already reconciles the pair the record called contradictory, and
the submission never cited it; the carve-out was also fail-open on a precondition nothing establishes).
**P6 PARK** (#305 is the discipline WORKING, and this ledger has ruled three times that is not a
sighting — `REJECTED.md`:1812-1814, :1226-1229, :1725-1726; #397's cycle 4 was owed by `harden`:193
regardless). All three accepted without argument.

Below: **P1, P4, P5 revised** against the gate's blocking objections, and **P7, which the gate itself
raised and which therefore has never been refuted by anyone.** P7 is the reason for this round.

---

## P7 · A claim-correction sweep needs a terminator that is not another grep (NEW — raised by the gate)

**Bar: (a) three records, AND (b) three rounds in one and two cycles in another.**

- **#305** `2026-09-07-…-305.md`:76-79: "'A wider grep terminates a claim-correction sweep' -> four
  successively wider greps each missed a surface (test javadoc, then the sole production entry point,
  then XML). What terminated it was a STRUCTURAL check needing no phrasing (any line naming #302 and
  #284 but not #305) plus deleting every count outright, and even that first shipped with three counts
  still standing." Cost: r3, r4 and r5 at "1 round" each (:42, :43-45, :46-47), and :74-75 — "three
  consecutive rounds were homes of one property. The sweep, not the edit, was the failing artifact."
- **#397** `…-397.md`:119-124: "A sweep for a corrected claim has now missed a home on three
  consecutive rounds of one change, each time in a file the correcting round had open. Grepping the
  claim's rarest token over the whole tree is not enough when the claim is a SENTENCE that can be
  rephrased; what caught it was re-running the mutation and reading which cases actually redden."
  Also :229-230, a third home the finding's own sweep missed "because the claim was rephrased there".
- **#379** `2026-09-08-…-379.md`:13: "'the measurement the ticket names has not been run' -> stale in
  4 homes (README, config.xml's operator-facing description, the GP constant's javadoc, Decision 77)
  after README alone was corrected. config.xml was missed because the sweep was doc-scoped. · cost: 2
  cycles."

**What existing text does NOT reach.** `pr-harden`:687-707 and `harden`:390 both ship the method — the
claim's rarest single TOKEN, over the whole tree rather than the docs — and both name two failure modes
(a phrase the formatting split; a DATA file missed by scope). Both end "treat no list of those
mechanisms as closed", which tells a reader the list will keep growing and gives no exit. Neither says
what ENDS a sweep, and two of the three records name a terminator independently. This is not
`REJECTED.md`:843-846's killed *prose-correction cycles* family (that one is about rewording
introducing new falsehoods) and it is not :1226-1229's *rule-not-followed* shape: on #305 the rule was
followed four times, at a round each.

**Proposed edit — append to `pr-harden`'s *Correcting a claim means finding every home of it*** (rounds,
so this is the document that owns both round-costed records):

> **And a sweep needs a terminator that is not another grep.** Widening the search is what a sweep
> reaches for, and widening does not converge: on #305 four successively wider greps each missed a
> surface — test javadoc, then the sole production entry point, then the module descriptor's XML — and
> three consecutive rounds were homes of one property, a round apiece. What ended that one was a check
> with no phrasing in it, every line naming the two earlier issue numbers and not this one, plus
> deleting the counts outright instead of correcting them. On #397 the rarest-token search missed a home
> on three consecutive rounds, each time in a file the correcting round had open, because the claim was
> a SENTENCE and had been rephrased there; what caught that one was re-running the mutation and reading
> which cases redden. So when a second sweep for one claim comes back clean, stop looking for the words:
> find a check whose subject is a STRUCTURE — an identifier, a number, a shape — or delete the claim and
> leave the method.

**Proposed edit — one sentence appended to `harden`:390** (cycles, so #379 is this document's instance):

> **And a sweep needs a terminator that is not another grep** — on #379 one claim was stale in four homes
> after the first was corrected, the survivor missed because the sweep was doc-scoped, at two cycles.
> `pr-harden`'s *Correcting a claim means finding every home of it* carries what ended two such sweeps.

**Prune answer.** Adds ~8 lines to `pr-harden` and 3 to `harden` and prunes nothing directly — but it is
the exit condition for a method both files already carry and both already describe as open-ended, and
its second terminator ("delete the claim") is the move `harden`:383-387 and `pr-harden`:290-293 already
prescribe, now reachable from the sweep that needs it.

---

## P1 revised · Which case hands the guard's subject its OTHER value

**Accepted from the gate, and the text is rebuilt around them:**

- **Placement (BLOCKING).** All three new measurements are pr-harden ROUNDS. `…-397.md`:336-337
  prescribes the home in terms: "Worth carrying into the fixer brief as a standing question". **So P1
  now lands in `pr-harden` only. No `harden` edit, and `harden`:242-250 is NOT touched** — which also
  answers the gate's objection 8: "and no mutation of production reaches it", the phrase "positive
  control", and `pr-harden`:287's "not covered by *If you ADD a guard*" all stay where they are.
  The submitted merge-and-delete is withdrawn.
- **The false consequence (BLOCKING).** "the suite is green either way" is refuted by `…-397.md`:212-213
  — "hardcoding `true` there DOES redden one case". The rule is now stated as the QUESTION.
- **The universals and the positional cross-reference (BLOCKING).** "only", "all of the guard", "every
  fixture", "never of the mechanism" and "The mutations above" are gone.
- **The overclaim (BLOCKING for the text).** "mutation-verified by its author" is false of #379
  (:21, "rested on review alone") and unsourced for #393. Now: "found by a fresh reviewer rather than
  by the author of the guard", which every record supports.
- Bar (b) restated honestly: **three blocking rounds in #397**, not four (:87, :156, :203 blocking;
  :232 "ZERO blocking"). Bar (a) is three records and is what this rests on.

**Proposed edit — `pr-harden`:287-289, the existing green-guard bullet, EXTENDED rather than replaced:**

> - **A guard that is supposed to stay GREEN is not covered by *If you ADD a guard*** — a negative
>   assertion passes whether or not its subject could ever arise, so build the case it exists for and
>   watch it fail. `harden`'s Termination carries the measurements (#360, #355).
>   **The general form is a question about the guard's SUBJECT rather than about the guard: which case
>   hands it the OTHER value?** A negative assertion is the instance where one value is never built; a
>   boolean, an arm of a split, and the order of a published pair are others. Where the fixtures agree
>   on that value the assertion is satisfied by a constant, so ask it of each guard you add, and ask
>   whether the mutation you just proved has a mirror nobody has run. Four rounds across three runs,
>   each blocking and each raised by a fresh reviewer rather than by the guard's author: a context test
>   asserting `Boolean.TRUE` at both legs, where `= true;` at both call sites stayed green; that same
>   flag's last hop, where substituting the retained flag-less overload stayed green while hardcoding
>   `true` reddened a case, so the hop was covered in one direction and not its mirror; a pair of
>   published numbers whose wire fixtures were `(n, n)` throughout, leaving the two assignments
>   transposable and green; and an ordering contract published over two arms whose tests drove one, so
>   hoisting the untested arm restored the defect it was written for with the suite passing.
>   **And read a mutation result as a measurement of the arms it moved rather than of the mechanism** —
>   one run published "removing this bound is byte-identical", true of the arm it ran and false of the
>   other, at a cycle and a round.

---

## P4 revised · Delete the false mechanism, and say the unattended rule rests on it

**Accepted from the gate (BLOCKING):** "load-bearing for nothing" is false. `pr-harden`:863-864 ("a
`claude -p` process exits when its turn ends … there the yield IS the death") plus :868 ("collect it in
the same turn") do rest on an inline return, and `…-PR393.md`:55 says so: an unattended run under the
observed behaviour "would have depended entirely on the gate". Also accepted: **prefer deletion**
(`skill-retro`:87-89), and **"observed both ways" is unsourced** — no record observes the inline
direction. Both dropped.

**Edit 1 — `pr-harden`:875-876, delete the asserted mechanism from the lead sentence:**

> **Collecting in the same turn means never polling afterwards.** Several `Agent` calls in ONE message
> run concurrently … [rest of the paragraph unchanged]

**Edit 2 — append to the unattended paragraph at `pr-harden`:862-874, as an open question and not a
diagnosis** (`skill-retro`:75-79):

> **And the rule above assumes the `Agent` call returns its report inline, which #393 measured it not
> doing** — every call in that run launched in the background. That run was ATTENDED, so the completion
> notification re-invoked the orchestrator and nothing was lost, and it recorded that an unattended run
> under the same behaviour would have depended entirely on the gate. Which behaviour an unattended run
> gets is not established, so treat the gate as the mechanism and same-turn collection as the intent.

---

## P5 revised · `started` is not a "reliable positive signal" — with the citation set the gate corrected

**Accepted from the gate (BLOCKING for the citations):** **#336 carries no such sighting** (its records
mention `started` only about a stale local `main` ref), and the ledger entry the submission inherited it
from is itself mis-cited. **FM2-700 must not be cited either** — the gate checked its record (:18-19) and
it never mentions the status endpoint, so P5a's own two-record basis does not support this half. **#397
is the MIRROR** (`started: false` for a module that genuinely had not started — the check telling the
truth) and is dropped from the skill text. **The gate found a supporting record inside this window that
the submission missed:** `2026-09-07-…-305.md`:83-85 — the stale expanded-module cache "with the omod
timestamp, module-status endpoint and cache marker all reading current. Only hashing the loaded class
against the built omod caught it."

Supported basis: **#340, #305, #393** — three records.

**Proposed edit — `verify-frontend-change`:57-58:**

> - **`started` is necessary and not sufficient — it is a signal about the module's lifecycle, not about
>   the bytes that run or the paths they map.** Hit the module REST endpoint (`/ws/rest/v1/module/<id>`)
>   or the manage-modules admin page and confirm the state is **started**; then drive one path the module
>   itself serves before concluding it is up. It read green over stale bytes on #340, and on #305 where
>   the omod timestamp and the lib-cache marker read current alongside it and only hashing the loaded
>   class against the built omod settled it — step 2's lib-cache deletion is the lever for that half.
>   On #393 it read `started=true` with a null `startupErrorMessage` while every REST path that module
>   serves returned 404, so it is not evidence the controllers are mapped.
> - **Also** scan the startup log for the module id: there must be **no** `ModuleException` / `Error
>   while starting module` / mapping or bean failure. Absence of a clear "started" line is *not* proof
>   of failure — a module can start while logging only benign warnings, so don't infer breakage from
>   the log alone.

**Prune:** the clause "rely on the state check above" is deleted from :58; the "(reliable positive
signal)" label goes. **Also owed and not a skill edit:** `REJECTED.md`:1341-1343 says the two records
are #340 and #336; #336 is wrong and FM2-700 does not carry the status-endpoint half either. Correcting
that line is part of this pass.

---

## Parked list, corrected per the gate

- **Isolated agent worktrees — re-parked at 2 records, not 1.** `REJECTED.md`:317-323 already parks
  "pr-harden is silent on where the FIXER works" (#250); `2026-09-08-…-379.md`:27 is the second. Still
  not shipped, on :318-322's own ground: #250's parking reason was "the orchestrator over-applied the
  rule; that is a run decision, not a document conflict", and #379's fixer was isolated "per the brief",
  i.e. by the orchestrator's choice. **REOPEN ON:** a run where `pr-harden` ITSELF tells the fixer to
  isolate.
- **Counts published in prose going stale — 4 records this window, carried and not proposed.**
  `…-305.md`:9-15 (three counts still standing inside the commit titled "counted nowhere", 1 round),
  `…-PR393.md`:18 ("api 1995 tests" → 1997), `…-397.md`:232-240 ("nineteen" against 20, "a dozen"
  against 16, and a third count of one population), `…-379.md` throughout. The rule exists
  (`harden`:388, `pr-harden`:294-304) and this is the *instruction-not-followed* shape
  `REJECTED.md`:1196-1198 cuts, so nothing is proposable — but skill-retro:70-71 requires the count to
  be carried, and the previous submission dropped it.
- **#229's "the verifier can green a change whose schema half never deployed" stays parked at 1.**
  `REJECTED.md`:1220-1226's reopen is "a second record"; #393 is not one — its mechanism is a stale omod
  serving 404, not a liquibase changelog skipped on a same-version redeploy. Checked, not met.
- Unchanged from round 1 and still below the bar: a brief's own factual claims (3 records, all caught at
  zero cost — reopen at `REJECTED.md`:1721-1726 explicitly NOT met by a third zero-cost catch); the
  measurement's input moving between arms (1 record, 2 incidents); editing the tree while a
  backgrounded build runs (1 record, 2 invalidated builds, no ledger entry against it); a line-scoped
  grep over a wrapped sentence (1 record, twice — and note P7 now carries the terminator half of it);
  `resolve-ticket` has no step for a gate that is not on main yet (1 record); P2's identical-head
  enumeration (1 record, 0 cost); P3's FINISH body rewrite (1 record, 0 cost); P6's delete-side
  every-home search (1 failure record).

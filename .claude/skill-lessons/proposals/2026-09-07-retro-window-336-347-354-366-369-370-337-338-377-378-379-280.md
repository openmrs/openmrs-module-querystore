# skill-retro submission — window of 13 records (2026-09-03 … 2026-09-07)

LAST = 2026-09-02. Records read (13):
336/PR368, 347/PR367, 354 (aborted at gate pass 2), 366/PR366, 369/PR369, 370/PR371,
2026-09-04-337/PR375, 338/PR376, 377/PR381, 378, 379/PR382, 2026-09-07-337/PR384, 280/PR383.
Linter: `skill-lint.py` — 10 files checked, 0 findings, exit 0.

Each proposal states which limb of the Step 3 bar it meets. Line refs `:N` are `REJECTED.md`.

## P1 · pr-harden State (429 paragraph) — state what four runs measured UNDER the hook; give harden Phase 2 the contract by pointer
Bar: two-or-more records — FOUR, and they are the evidence :1620 parked for
("**REOPEN ON:** a record of a 429 met under the hook"). It is also the home the 2026-09-02
DEFERRED P2 was carried to (:1258-1260, "harden Phase 2 delegates four agents and carries no retry
contract of its own — 2 records").
- 336: the hook "refused a `model: sonnet` retry after a session-429 killed an agent, which is correct
  and is now the documented state — the leaner-brief retry succeeded on the session model".
- 366: "The skill's documented lever (retry on a cheaper model) is now refused by a PreToolUse hook,
  leaving 'leaner brief' and 'wait for the stated reset'. Both applied; the retry converged."
- 370: "a **weekly** quota 429 (not a session one): 'wait until the stated reset' was useless (reset
  was the next day) and the per-call `model` lever is hook-refused. A plain retry with a leaner brief
  succeeded, which suggests the first failure was a burst rather than the weekly cap actually being
  exhausted — the two are indistinguishable from the error text."
- 379: "harden Termination vs a 429: cycle 5's confirming agent died on a session rate limit and the
  per-call model override is hook-refused, so the documented retry lever was unavailable."
Edit (pr-harden :858-869): keep the refusal and #339's residue; say the measured remainder is a leaner
brief (four deaths, four convergences under the hook) and bound the WAIT by whether the stated reset is
inside this run's horizon — a weekly cap states one a day out and the error text does not separate it
from a burst, so both retries must not be spent waiting.
Prune in the same edit: the two-run history of the retired cheaper-model lever compresses to one clause
(the measurement is kept, not deleted), and #354's confounded wait measurement goes — :1623-1626
already disclaims it as a witness ("that retry also changed model").
Edit (harden Phase 2, one sentence): a Phase 2 agent that dies takes pr-harden's State contract — clear
the await, retry twice with something changed, then the labelled override. Two of this window's four
deaths were harden's own (336, 379) and harden carries no contract.

## P2 · pr-harden Step 1 base drift — a third class: a shared BUDGET the merge consumes
Bar: two-or-more records — SEVEN. :1655 parks it at 1 record (#355, "A repo size/budget guard
overflowed by MERGING the base").
- 2026-09-07-337: "base 69,772; #379 added 2,209 -> 71,981; mine added 2,198 -> 71,970; budget 72,000.
  Each fits alone, neither with the other … Directives survive in javadoc + ADR Decision 78; **the
  instruction-file binding is lost.**"
- 280: "main alone left 19 bytes of headroom, so the second branch could not record a rule of ANY size.
  Resolved by trimming this branch's own prose first … and raising to 75,000 with the reasoning written
  into the guard."
- 336: "`main` alone was at 84,961 of 85,000, so no rule of any size fitted. Resolved the way the file's
  own 'Documenting a decision' section prescribes — cut EVIDENCE, keep the RULE."
- 347 (15 bytes under), 377 (201 bytes under, deferred), 379 ("the third merge from main put BOTH
  instruction files over their size budgets"), 369 (the merge pushed CLAUDE.md over).
Edit: name the class after the two existing ones — the base consumed a shared budget, so a change that
fitted before the merge does not after, and what reddens is prose rather than code. State the two moves
the runs used: trim your OWN added prose, or move the rule to the code it binds. No count published.

## P3 · pr-harden Step 1 identifier paragraph — the sequence's own INDEX is part of the sweep
Bar: two-or-more records — FOUR.
- 369: "`adr.md`'s TOC is a merge hazard: in merge 1 git silently kept only one of two lines added at the
  same insertion point, dropping main's, with nothing in the build failing"; and "**`main` has been
  missing its own Decision 70 TOC entry since #367** … because `ProjectInstructionsGuardTest` validates
  CLAUDE.md-cited decisions rather than TOC completeness".
- 280: "[pr-harden r3] ADR Decision 78 had eight citations and no entry in the ADR's own table of
  contents; the guard checks the heading, not the index."
- 378: "ADR TOC (75 entries) — self-caught, but only because I looked for one."
- 2026-09-07-337: the by-number sweep's eight homes included "a TOC entry".
Edit: one sentence — the sweep includes the sequence's own index, and after a merge check the index for
BOTH numbers, because two entries added at one insertion point resolve to one line with nothing failing.

## P4 · resolve-ticket Step 1 table + pr-harden Step 1 — prescribe an invocation that reads the issue
Bar: two-or-more records — THREE (236, 255, 347), and the route is the one :1146-1152 prescribed for a
third: not a machine fact but a fact about the DOCUMENT, "since the skills prescribe the failing
invocation themselves — `resolve-ticket`'s URL table and `pr-harden`'s 'A GitHub issue via
`gh issue view <m> --comments`'".
- 347: "`gh issue view` returns EMPTY in this environment (exit 0, no output). `gh api
  repos/O/R/issues/N` works. Cost: two wasted calls; every subsequent agent brief had to carry the
  workaround."
Edit: prescribe `gh api repos/<owner>/<repo>/issues/<n>` and `…/issues/<n>/comments`, with a half-clause
that empty output at exit 0 is that failure rather than an empty ticket.

## P5 · pr-harden Step 1 — an isolated agent checks out the SHA, never the round's ref NAME
Bar: two-or-more records — 370 and 366/369; :1657 parks a third mechanism of the same family at 1
record (#355, "a `pr-<n>-r<round>` fetch ref ending up checked out").
- 370: "reviewer agents in isolated worktrees could not `git fetch origin pull/371/head:pr-371` because
  a SIBLING agent's worktree already held that branch name. Two reviewers reported it. Briefing them to
  check out the SHA rather than create a named branch fixed it; worth stating in the brief template."
- 366: "The branch was held by FOUR orphaned lens worktrees … so `git checkout` of the PR branch was
  refused." 369: the same in both its tickets.
`harden`:140 already carries the sibling half for its own agents (`--ignore-other-worktrees`).
Edit: the orchestrator fetches the round's ref as today; the brief names the SHA and the agent checks
that out detached.

## P6 · harden Phase 2 — reap the cycle's lens worktrees, and never adopt content from a leftover one
Bar: two-or-more records for the orphans (366, 369). The disk pair is stated as consequence, NOT as the
corroborating count, because 377 and 379 may describe one physical disk-full event across two runs.
- 366: "A killed run leaves these behind and nothing reaps them."
- 369: "For #348 three of four were NOT byte-identical to the head: they held dead agents' mutations — a
  swapped prompt clause, a deleted clause, and a `System.err.println` probe with an added map."
- 377 (consequence): "the disk filled mid-run from six isolated agent worktrees each running a full
  [maven build] … and Bash became unable to create its own output file, so no command, edit or state
  write was possible. The run had to hand back … and could not even record the override. Isolated-worktree
  review agents are not free." 379 records the same inability to write its own state.
Edit: at cycle close remove the cycle's lens worktrees (`git worktree remove --force`), and treat a
leftover one as a dead agent's tree rather than as the head.

## P7 · gate-state `pr-set` — clear the per-PR ledger when the PR NUMBER changes
Bar: clause 3, a gate script contradicting the contract its skill states (valid on one sighting), plus a
mechanical verification I ran.
- 2026-09-04-337: "pr-harden Step 0: the state entry for this worktree carried three reviewed_shas from
  the EARLIER #337 run (PR #345) in the same reused worktree. `pr-set` preserves that field, so the
  ledger silently spanned two PRs. Cleared explicitly; worth a note that a reused worktree inherits a
  ledger."
- Verified in `~/.claude/pipeline/gate-state`: `pr-set` assigns `e["pr"]` and only `setdefault`s
  `reviewed_shas` / `declined`, so both survive a change of PR. `harden-set` guards the analogous reuse
  hazard for `head` explicitly (same owner AND a monotonic cycle) — so the two subcommands disagree about
  a reused checkout. pr-harden Step 0 licenses adoption only where `pr` matches or is null, and Step 1
  compares the incoming head against `reviewed_shas`' last entry.
Edit: in `pr-set`, where an existing non-null `pr` differs from `--pr`, drop `reviewed_shas` and
`declined` and print what was dropped; `pr: null` (the resolve-ticket handoff) keeps them. Two
`pool-test.py` cases, verified to fail before and pass after. One clause in Step 0.

## P8 · pr-harden "Editing by script" — a fourth silent failure: the write that never happens
Bar: two-or-more records, a round each.
- 336: "Two `Decision 68` -> `69` replacements printed `ok` and never reached disk: the script called
  `open(path,'w')` only after its loop, and an assertion later in the loop threw. **The blocking finding
  of round 1.** · cost: 1 round"
- 280: "A python edit script whose write followed a failing assert silently lost three edits … The commit
  message announced edits the diff did not contain; a later review pass found it. The skill's 'assert
  before replacing, verify by reading back' covers the first half but not 'one script, several edits, one
  write at the end'."
Edit: one bullet — write each replacement as you make it, so a printed success and the file cannot
diverge; a script that batches several edits behind one write reports edits an abort never made.

## PRUNE (paired with no addition) · resolve-ticket Step 1 — delete "or no LLM endpoint for a module that needs one"
- 2026-09-04-337: "resolve-ticket Step 1 pre-flight checks for 'an LLM endpoint' by looking for a
  listening port / llama-server process. On this module the engine is local and self-launching, so the
  check reports a blocker that does not exist, and it was carried into two agent briefs as fact"; and
  "Pre-flight: 'no LLM endpoint on this machine' -> mis-scoped. The engine is local and self-launching on
  port 18085 with a bundled model under the standalone's appdata."
Deleting rather than rewording, per Step 4. The retiring measurement is recorded here and in the commit.

## Parked — observed, not yet actionable, with counts
- **Step 3's fourth outcome** (the gate settles NEGATIVELY and no in-scope code change survives):
  2 records — 354 (aborted; "The rule held; recording it because the shape recurs") and 338 ("The skill's
  three outcomes cover 'settles' and 'leaves open' but not 'settles negatively, and the ticket then has no
  accepted code change'", which became a measurement deliverable and converged). Both cost 0 rounds and
  both say the rule held; :1735's precedent treats a zero-cost catch as the step working.
  **REOPEN ON:** a record where the missing outcome cost a round or produced a wrong abort.
- **Byte-identity is the wrong test for a comment-only push; `javap -c` is:** 1 record
  (2026-09-07-337, cost 0, "The skill's byte-hash advice needs this distinction"), supported by 369's
  compiled-class-diff standard. **REOPEN ON:** a second record.
- **`--count-edits`' upstream fallback answered by hand every cycle:** 3 records (347, 370,
  2026-09-07-337). All three are the fallback printing its own note and the run answering it — the
  behaviour the 2026-09-02 fix shipped; cycle 1 has no recorded head by construction.
  **REOPEN ON:** a record where the note was absent, or the wrong number reached the gate.
- **Recording an await is a separate call from spawning:** 1 record (2026-09-07-337; the gate caught the
  yield, cost 0).
- **A run record with a raw build log interpolated into a bullet:** 1 record (377, 638 KB; read here by
  section extraction). **REOPEN ON:** a second, or a retro that cannot read a record whole.
- **Prose-correction cycles: ~28 records** — previous ~24 (:1633), plus 338 (14 cycles, "applying it at
  cycle 6 rather than cycle 11 would have saved roughly five cycles"), 378 (5), 2026-09-07-337 (9), 377.
  Remedy still killed; deletion-over-rewording is the shipped rule and 338 records it as what worked.
- **`git checkout -- <path>` losing uncommitted work: ~22 incidents / 18 records** — previous ~21/17
  (:1628), plus 378 ("The skill documents exactly this and I did it anyway"), again the orchestrator's own
  probe with the commit skipped. :81's reopen (an incident where "commit before probing" WAS followed) is
  still unmet.
- **Rate-limit agent death: 15 records** — previous 11 (:1635), plus 336, 366, 370, 379.
- **pr-harden's round cap raised past its default: 3 records** — previous 2 (:1637), plus 379 (4 -> 5,
  converged at 5).
- **Orphaned lens worktrees, the disk half:** 2 records (377, 379), possibly one event; carried inside P6
  rather than counted.

# Retro window · #305/PR385, #379/PR386, PR393(#388), #397/PR398 · 2026-09-09

Records read: 4, all since `LAST` = 2026-09-07 (written 13:55; `2026-09-07-...-305.md` is 18:59 and
falls in this window). Linter: 10 files, 0 findings.

- `2026-09-07-openmrs-module-chartsearchai-305.md` — pr-harden via resolve-ticket, 6 rounds, converged
- `2026-09-08-openmrs-module-chartsearchai-379.md` — resolve-ticket + harden + pr-harden, 3 rounds / 4 cycles
- `2026-09-08-chartsearchai-PR393.md` — pr-harden, 3 rounds, converged
- `2026-09-09-openmrs-module-chartsearchai-397.md` — resolve-ticket, 4 rounds + blocking-only, 4 harden cycles

---

## P1 · A mutation measures only the arms it exercised; a uniform fixture population hides the mirror

**Bar: (a) three records AND (b) four rounds in one of them.**

- **#397**, four consecutive rounds, each blocking, each the same shape — the record names it itself:
  "Every round's fix added a guard, and the next round's blocking finding was that the new guard was
  one-directional or unpinned." Instances: `r2-1` the context test "asserts `Boolean.TRUE` at both legs,
  so nothing observes a call site handing FALSE — `= true;` at both sites leaves 2192 green";
  `r2-2` "`findingSubjects`' split at the `:` is a no-op in every arrangement the suite builds …
  `subjects.add(key)` leaves 2192 green"; `r3-1` "Swapping both to the retained flag-less 3-arg
  overload leaves 2194 green … The reviewer also established the asymmetry: hardcoding `true` there
  DOES redden one case, so the hop was uncovered in one direction only"; `r1-3` "the measured clause is
  asserted by substrings, so an ADDED imperative ships green". The record's own generalisation:
  "The class of defect that survives is **the mirror of the mutation you just proved.**"
- **#379**, pr-harden r1, BLOCKING, cost 1 round: "every wire fixture was `ActiveOrderClaims(n, n)`, so
  transposing the two `map.put` arms in `serializeActiveOrderClaims` shipped GREEN — the two published
  numbers rested on review alone."
- **#379** again, cost 1 harden cycle + 1 pr-harden round: "'#377's byte-identical measurement of
  removing the next-occurrence scan bound' -> true of the ACCUSATION only; substituting it moves an
  uncited claim to cited with the whole suite green." A published mutation result true of one arm,
  written as a claim about the mechanism.
- **#393**, r1, BLOCKING, cost 1 round: "The ordering contract is published over BOTH cross-reactivity
  arms (README, nested CLAUDE.md, ADR 82) but every test drove only the ATC-class arm … hoisting ONLY
  `CrossReactivityGroup.sharedGroup` into pass one leaves chip text byte-identical, moves emission
  order, restores #388's defect for group-related allergens, and passes the whole suite."

**What existing text does NOT reach.** `harden`:235 lists mutations *of the guard* (deleted, arms
swapped, comparison loosened, semantically equivalent rewrite); :261-276 mutates the guard's *text
subject*; :277-283 mutates the *value under an asserted key*; :251-260 asks for a *sibling*. All four
ask what else to mutate. None asks what the FIXTURES could have observed — and in all five instances
above the guard was mutation-verified by its author and the suite was green, because every fixture
agreed on the value the guard is about. `harden`:46-75 is the closest ("ask what the FIXTURE can
express"; "ask of any clean or zero result what its inputs could not have produced") and is scoped to
a *stub's* behaviour and to *clean or zero* results; every instance here is a green PASSING guard.

**Proposed edit — `harden` Termination, merged INTO the existing green-guard bullet at :242-250, which
this subsumes** (a negative assertion is the special case where one of the subject's two values is
never built):

> - **A guard observes only the values its fixtures actually take, so name the case that observes each
>   one.** The mutations above are all of the guard; this one is about its SUBJECT. Where the guarded
>   thing is two-valued — a boolean, an arm of a split, an order of a pair — ask which case hands it the
>   OTHER value, and if every fixture agrees on it the guard is satisfied by a constant and the suite is
>   green either way. Measured: a context test asserting `Boolean.TRUE` at both legs left `= true;` at
>   both call sites green; a pair of published numbers whose every fixture was `(n, n)` left the two
>   assignments transposable and green; a contract published over two arms was driven on one, so hoisting
>   the untested arm restored the defect with the whole suite passing. The mirror of the mutation you
>   just proved is the one that ships. **A negative assertion is this case with one value never built** —
>   "nothing was injected", "this name does not appear" passes whether or not its subject could arise, so
>   build the case it exists for and watch it FAIL (#360: a control's chart came from a helper that wires
>   no validator, so the collection it asserted was empty could not have held anything, and two review
>   rounds rather than the mutation check found it; on #355 a verifier briefed to look for a partner the
>   shipped data does not carry re-drove the contract with one it does, plus a control, rather than
>   reporting the absence as a result). **And a mutation result is a measurement of the arms it moved,
>   never of the mechanism** — one run published "removing this bound is byte-identical", true of one arm
>   and false of the other, at a cycle and a round.

**Proposed edit — `pr-harden` fixer brief.** Delete the standalone green-guard bullet at :287-289 and
put its pointer on the *If you ADD a guard* bullet at :275-286 instead:

> **And ask which case observes the OTHER value of the thing the guard is about** — a boolean asserted
> TRUE at every leg, a pair whose every fixture is symmetric, one arm of a two-arm contract: the mirror
> of the mutation you proved is what ships green. A guard meant to stay GREEN is that case with one
> value never built. `harden`'s Termination carries both and the measurements (#360, #355).

**Prune answer.** Two bullets become one in `harden` (:242-250 folded in, its #360/#355 measurements
kept verbatim); one bullet is deleted in `pr-harden` (:287-289) and becomes a clause on its neighbour.
Net ≈ +3 lines across the two files for the most-corroborated lesson in the window.

---

## P2 · pr-harden's identical-head guard enumerates causes that exclude one the skill itself licenses

**Bar: (c) — a skill contradicting itself. One instance is enough and this is a fact about the document.**

- **#393**: "pr-harden:'The round' step 1 — the guard 'if the new head EQUALS the previous round's, stop
  and establish why' fired at r3 and the enumerated causes did not cover this one: r2's ONLY finding
  named the PR description, which is not in the tree, so no commit was made and the sha legitimately did
  not move. … The skill already says a description finding 'still counts as the round's fix and the
  round proceeds normally' — but that sentence and the identical-head guard are in different sections and
  do not reference each other."

Verified against the text: :138-141 enumerates three causes ("the round pushed no commit (the fixer
declined everything — that is a *did not converge*)", "the push had not landed", "the ref was created
outside a round at all"). :333-335 says a description finding "still counts as the round's fix and the
round proceeds normally". A round whose only finding was the body satisfies :333-335 and lands in the
first enumerated cause, which classifies it as *did not converge*. The two passages disagree.

**Proposed edit — `pr-harden`:138-141**, adding the case and making the list non-exhaustive, which is
what the skill's own :300-301 asks of an enumeration ("treat an exhaustive list as worse than none"):

> So stop and establish why before spawning — the round pushed no commit (the fixer declined everything
> — that is a *did not converge*, see **Termination**, not a free round), the round's only fix was to the
> PR DESCRIPTION, which is not in the tree and moves no sha (see step 4; that round proceeded normally
> and #393's r3 met exactly this), the push had not landed when the fetch ran, or the ref was created
> outside a round at all — and treat none of those as the closed list.

**Prune answer.** Subsumes nothing; +1 line. It removes a contradiction rather than adding a rule.

---

## P3 · FINISH tells you to rewrite a body a round may just have reviewed

**Bar: (c) — the instruction contradicts the skill's own handover principle.**

- **#393**: "pr-harden:FINISH — 're-derive the PR description against the merging head' is in tension with
  'FINISH does not edit the cleared sha' when the body has just been reviewed clean by r3 and every figure
  in it has been re-measured on that head. Resolved by re-checking each figure rather than rewriting the
  prose, on the ground that a wholesale rewrite would substitute unreviewed text for reviewed-and-verified
  text. Worth stating explicitly in the skill either way."

:595 says "Rewrite it whole here, re-measuring every figure in it rather than carrying one forward."
:1128-1130 is the anti-pattern "Don't hand over a sha nobody reviewed", and :333-340 makes the body a
reviewable artefact a round can find blocking findings in. Rewriting a body a reviewer cleared on this
head hands over unreviewed prose, which is the same defect one artefact over.

**Proposed edit — append to `pr-harden`:591-596:**

> **Unless a round has just reviewed the body on this head** — then re-check every figure in it and leave
> the prose, because a wholesale rewrite substitutes unreviewed text for text a reviewer cleared, which is
> *Don't hand over a sha nobody reviewed* one artefact over. Rewriting whole is for a body carrying
> patches no reviewer read (#393 r2's finding named the body; r3 then reviewed it clean).

**Prune answer.** Subsumes nothing; +2 lines.

---

## P4 · Delete "the `Agent` call RETURNS the report" — measured false, and the same paragraph says otherwise

**Bar: (c), and the deletion is what `harden`:383 / `pr-harden`:290-293 prescribe for an unsupported clause.**

- **#393**: "pr-harden:State — every Agent call in this run launched ASYNC despite the skill's 'collect it
  in the same turn / the Agent call RETURNS the report'. Attended session (remote-control, no unattended
  marker), so the completion notification re-invoked the orchestrator each time and nothing was lost; an
  unattended run under the same harness behaviour would have depended entirely on the gate."

:875-876 asserts the harness property. :879-881, four lines later, describes the opposite behaviour and
the actual mechanism: "Launching async and then blocking on `TaskOutput` collects nothing extra — the
report arrives by itself in the completion notification's `<result>`." The operative rules (:868 never
end a turn with an agent outstanding; never poll) do not rest on the asserted property, so the clause is
load-bearing for nothing and false where a record measured it.

**Proposed edit — `pr-harden`:875-876**, replacing the asserted mechanism with the rule it was standing in for:

> **Collecting in the same turn means the report reaches you in the completion notification, never in a
> poll you run afterwards.** Whether the `Agent` call returns inline or launches in the background is the
> harness's to decide and has been observed both ways (#393: every call in that run launched async and
> nothing was lost, because the notification re-invoked the orchestrator — in an ATTENDED session, which
> is the half that does not transfer). Several `Agent` calls in ONE message run concurrently, so a wave
> keeps its parallelism …

**Prune answer.** Deletes a false factual claim about the harness and keeps the rule. Net ≈ 0 lines,
one fewer unsupported claim.

---

## P5 · verify-frontend-change step 4 still calls `started` a "reliable positive signal"

**Bar: (a) — four records, and the previous retro RECORDED this as owed residue rather than fixing it.**

`REJECTED.md`:1341-1343, from the 2026-09-07 pass: "**Residue recorded:** verify-frontend-change step 4
still calls the `started` state check the 'Primary (reliable positive signal)' — the signal both #340 and
#336 show reading green over stale bytes; the new bullet says so at the deploy step rather than re-writing
step 4."

This window adds a **new failure direction** — not stale bytes but no mapping at all:

- **#393**: "The inherited instance was serving 404 on every chartsearchai REST path from a Sep-7 omod
  while `/ws/rest/v1/module/chartsearchai` reported `started=true`, `startupErrorMessage=null`. So the
  module status endpoint is not evidence that a module's controllers are mapped."
- **#397**: "after a restart the module comes up `started: false` with no error message and needs an
  explicit start" — the negative reading carries no cause either.

The text as it stands tells a reader to "rely on the state check above" (:58), which is the sentence both
records contradict. `verify-frontend-change`:35-41 already says step 4 "cannot see this" for stale bytes;
step 4 itself was not updated.

**Proposed edit — `verify-frontend-change`:57-58:**

> - **`started` is necessary and not sufficient — it is a positive signal about the module's lifecycle, not
>   about the bytes that run or the paths they map.** Hit the module REST endpoint
>   (`/ws/rest/v1/module/<id>`) or the manage-modules admin page and confirm the state is **started**; then
>   drive one real path the module itself serves before you conclude it is up. Measured green over stale
>   bytes on #340 and #336 (see step 2), and measured `started=true` with a null error while every one of
>   that module's REST paths returned 404 on #393. In the other direction a restart can leave it
>   `started: false` with no error message at all, needing an explicit start (#397), so the negative
>   reading carries no cause either.
> - **Also** scan the startup log … [existing sentence, minus "rely on the state check above"]

**Prune answer.** Corrects a false claim the ledger already recorded as false; the last clause of :58
("rely on the state check above") is deleted. Net ≈ +4 lines in the smallest of the four governance files.

---

## P6 · The every-home search runs in both directions: deleting prose needs the search that correcting it needs

**Bar: (a) two records — one showing the discipline working and naming its failure mode, one paying for
its absence.**

- **#397**, harden cycle 3 Phase 2 → cycle 4: "deletion carried a failure mode nothing before it had:
  cutting the only home of a fact. Requiring a per-deletion ledger (what was cut, where the fact lives
  now, the grep that FOUND it there) is what made it safe; the two facts with no other home were MOVED,
  not dropped." And: "**Cycle 4 verified that independently and found one that had been missed** — a fact
  living nowhere." Cost: one cycle.
- **#305**, Declined: "Failure mode of declining: a later reviewer cannot tell from the file alone whether
  a trimmed reason was moved or lost — which is why the falsifiable half WAS done (every dropped clause
  checked for another home; the one orphan is in `docs/adr.md` Appendix A with its date and provenance
  commit)."

`harden`:383-387 instructs deletion ("delete the unsupported clause", "delete the CLAIM SHAPE") and
:390 gives the every-home search for CORRECTING a claim. Neither says what to check before a deletion.
`pr-harden`:186-189 says "Trim your OWN added prose, or move the rule to the code it binds" without
saying how to tell a move from a loss. `skill-retro`:Step 4 carries this rule for its own domain
("Never delete a measured rule without recording the measurement that retires it"), which is evidence
the shape is right and evidence it is missing from the skill that does the deleting.

**Proposed edit — append to `harden`:383-387 (the *Don't rewrite prose faster than you verify it* bullet):**

> **And the every-home search below runs in the other direction too: correcting a claim means finding
> every home of it, deleting one means proving there is another.** For each cut, name where the fact now
> lives and the grep that found it there, or say it had none and MOVE it instead. A cycle that deletes is
> the first one able to cut the only home of something, and the cycle after it is where that surfaces
> (#397 lost one that way and the next cycle found it; #305's trim under a size budget checked every
> dropped clause and recorded the one orphan with its provenance).

**Prune answer.** Subsumes nothing; +3 lines. It is what makes the pruning the skills already prescribe
safe, and this retro is itself applying it.

---

## Below the bar — parked, with counts

- **A brief's own factual claims going unchecked: 3 records (#338, #355, #397), all three caught by the
  agent unprompted at no round cost. REOPEN NOT MET and re-parked.** `REJECTED.md`:1721-1726 killed this
  in 2026-09-02 and set the condition: "a record where a false brief claim was NOT caught … or one that
  cost a round or a cycle. A second zero-cost catch is evidence the verifier step works, not that it needs
  text." #397 supplies a third zero-cost catch — two invalid instruments in one brief (byte-identity as
  the test; reading finding subjects off `safetyWarnings[]`), both refused by the verifier, which
  substituted better ones. Count 3. **Same condition stands.**
  - **New and narrower, and its home is not a skill:** #397 names the class as "a plausible re-expression
    of a production predicate produced a confident wrong number … `CLAUDE.md` already forbids it in
    scripts, and a subagent BRIEF turns out to be the same hazard with no rule covering it." That is an
    edit to the *project's* instruction file (extend "in a script" to cover a brief), not to a skill, and
    the root file is at its size budget. Reported, not proposed here.
- **An isolated agent worktree's output and its shared branch ref: 1 record (#379), 2 incidents, ~2 steps.**
  `harden`:127-128 recommends `isolation: "worktree"` and never says what an isolated agent's product is:
  "the cycle-2 confirming agent committed on `fix/379-…`, moving the orchestrator's branch pointer under
  it"; and "the fixer ran in an isolated worktree and left its work UNCOMMITTED there, per the brief; the
  orchestrator had to `git -C <agent-worktree> diff | git apply`. The skill says 'leave your edits in the
  working tree' without noting that an isolated worktree makes that a different tree." Steps, not rounds
  or cycles. **REOPEN ON:** a second record, or one costing a round or a cycle.
- **The measurement's INPUT can move between arms with nothing reporting it: 1 record (#397), 2 incidents.**
  A restart emptied the retrieval index — "`chartMode` still read `fullChart` while the chart dropped from
  ~348 records to ~9 … the first shipping-artifact arm was measured against a different prompt and was not
  comparable"; and the index REBUILD "shifted the chart text by a uniform 12 tokens, which re-worded 11 of
  14 greedy answers and moved the completeness cell by one in EACH direction". The record names the better
  instrument — "a cross-build differential on a quantity the change is known to move, with the cells the
  change cannot reach as the drift control". `harden`:63-68 has the sibling rule for REPEATS ("name what is
  reset between repeats"); nothing covers ARMS. Cost was one discarded arm, not a round or a cycle, and the
  project's own eval README already carries the rule its author then ignored — which is the
  *instruction-is-not-the-lever* shape `REJECTED.md`:1196-1198 cuts. **REOPEN ON:** a second record, or a
  round or cycle spent.
- **Editing the tree while a build you backgrounded is running: 1 record (#397), 2 invalidated builds.**
  "Running `mvn` in the background and then editing files raced the build twice, producing two invalid red
  results that cost a re-run each." `harden`:164-168 / `pr-harden`:924-934 forbid exactly this for a
  delegated AGENT reading the tree; a backgrounded build is the same reader. One clause away, and two
  re-runs are not two rounds. **REOPEN ON:** a second record.
- **A line-scoped grep cannot see a wrapped SENTENCE, and reports absence: 1 record (#397), twice in one
  run.** "A sweep for a SENTENCE must flatten whitespace; a line-scoped grep over wrapped prose is a
  fail-open check — it reports absence, which is the direction that invites writing a 'correction' over
  something already correct." The hazard is already named (`pr-harden`:696-700, `harden`:390: "a phrase the
  file's own formatting has split … does not match what you typed") and the existing prescription —
  search the rarest single TOKEN — is immune to it by construction, so what is missing is only the method
  for the case where you insist on the sentence. An existing instruction not followed. **REOPEN ON:** a
  record where the single-TOKEN method itself failed this way.
- **`resolve-ticket` has no step for "the gate you are told to work against is not on main yet": 1 record
  (#397).** "Merging main into that branch first, renumbering to 83 and fixing its five pointer sites was
  prerequisite, not scope." **REOPEN ON:** a second record.
- **Not parked, reported as the shipped rules working:** #305's `grep -E "^\[INFO\] Tests run:"` dumping
  ~150 lines ("The repo's own rule … is the one that was right"); #393's entry via `/resolve-ticket` with a
  PR URL, settled by that skill's own *when NOT to use* at no cost; #397's quality lens finding eleven items
  five correctness passes missed, which is `harden` Phase 2 working; #379's Decision-77 precedent for a
  directive that would not fit the root instruction file.

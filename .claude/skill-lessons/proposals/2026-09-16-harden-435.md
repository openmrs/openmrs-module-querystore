# Retro proposals · 2026-09-16 · from run record 2026-09-16-openmrs-module-chartsearchai-435.md

## P1 · KILLED by the refuter · skill-retro Step 1: state that a standalone `harden` run leaves no record
**Verdict: KILLED** on three grounds (bar (c) not met; `skill-retro`:10 already bounds the coverage
claim; the second sentence is unavailable from the corpus and points the other way). A patch-level
CORRECTION was applied in its place. Full reasoning in `REJECTED.md`, 2026-09-16 block.
Step 1 says records are "written by the skill that ran — or, for a run that ended without writing
one, by the `ticket-pool` driver". A `/harden` invoked directly by an operator satisfies neither:
`harden` has no capture step, and there is no driver. The 2026-09-16 run (10 cycles, ~20 claims
refuted by measurement, 12 of 19 commits with zero non-comment lines) reached this store only because
the operator asked a question that prompted a hand-written record.

Proposed text, in Step 1 after the sentence naming the driver:

> A `/harden` invoked on its own writes no record and has no driver, so its evidence reaches this
> store only if someone writes it by hand. Read the corpus as under-sampling standalone harden runs,
> which is where the prose-correction class is heaviest.

Bar claimed: **(c) a skill contradicting itself** — Step 1 asserts a capture coverage the pipeline
does not have, decidable from the documents alone.

Prune offered: none identified. Net +3 lines, justified by the second sentence, which changes how a
later retro reads its own counts.

## Observed, not yet actionable

**O1 · A mechanical detector for the self-rewrite signature — attempted, not calibrated.**
`harden`:401 ("delete the unsupported clause instead of replacing it with a better-sounding one …
several cycles in a row of this is the signature") is the rule this run violated for four cycles, and
this ledger's standing note on a twice-violated rule is that "any future remedy is probably
mechanical, not textual" (REJECTED:30). A line-granularity detector was written and run over the
run's own history: for each commit, is the diff comment-only, does it remove comment lines an earlier
commit of the same run added, and does it add comment lines in their place?

It does not work, in both directions, measured on this run:
- it flags cycles 8 and 9, whose edits were pure DELETIONS — cutting a clause out of a wrapped comment
  reflows the neighbouring lines, which is indistinguishable from a replacement at line granularity;
- it exempts cycle 7, which carried one code line, and cycle 7 is exactly the cycle that shipped a
  false claim through the documentation route.

A word-granularity version might separate a reflow from a replacement. Recorded so the next attempt
starts here. Script: `2026-09-16-selfrewrite.py` in this directory.

**O2 · Prose-correction cycles: this run is the 17th record**, and the sharpest: 12 of 19 commits
changed zero non-comment lines, and the whole run after cycle 1 produced 33 lines of code. Both rules
that would have ended it early were already shipped (`harden`:401 delete-don't-reword, :406 the
universals list) and both were violated by the orchestrator until cycle 7. No new remedy proposed.

**O3 · The documentation route shipped a false claim.** Cycle 7 was documentation-classified and
confirmed by a single agent; it rewrote the `assertCarriedWhole` paragraph and shipped "This is the
assertion that reddens on it", refuted by measurement at cycle 8 (`git show 7a080eed` carries the
removed and added wording). This matches the REOPEN condition recorded against the killed P1-R routing
clause (REJECTED:2779). **Not proposed**, on the ground that killed a sibling proposal at REJECTED:25-27:
the false claim was shipped by the ORCHESTRATOR applying a finding, not by the confirming agent, so
briefing text at that site would not have reached it, and the orchestrator-facing rule already exists
and is the one that was violated. Count: 1.

**O4 · Concurrent maven in one checkout.** A foreground `mvn -pl omod test` run while a background
`mvn clean install` was in flight in the SAME tree invalidated the background build. Cost: one build
re-run and a moment of treating its green as evidence. Count: 1.

**O5 · A concurrent pipeline run switched the shared checkout's branch mid-session.** Observed at the
end of this run: the chartsearchai clone was on `finding-partner-coverage-log-phi` with another run's
harden rounds on it, while this run's branch and remote were intact. Nothing was lost, and analysis
continued against the ref rather than by checking out. Count: 1.

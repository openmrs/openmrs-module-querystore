# skill-retro submission — 2026-09-13 (window: #374/PR411, #379/PR414, #294/PR410 CONTINUATION)

## Window

Three pieces of evidence, all unmirrored in the source repo at the start of this pass:

1. `2026-09-13-openmrs-module-chartsearchai-374.md` — resolve-ticket + harden + pr-harden,
   11 harden cycles, 2 pr-harden rounds, converged.
2. `2026-09-13-openmrs-module-chartsearchai-379.md` — resolve-ticket + pr-harden, 9 harden
   cycles, 2 pr-harden rounds, converged.
3. `2026-09-12-openmrs-module-chartsearchai-294.md` — the CONTINUATION section only
   (lines 106-180 live). The 2026-09-13 ledger block covered #294's first half and said
   explicitly "It is in-progress, so its completion is not covered here."

Linter: 10 files, 0 findings.

**A correction this pass owes on itself, before anything else.** Step 6's battery was first run
against `~/Projects/openmrs/openmrs-module-querystore` and reported three drifts —
`ticket-pool/SKILL.md` (0.21.0 vs live 0.22.0), `pipeline/pool-run`, `pipeline/pool-test.py` —
plus an unmirrored `skill-lessons/artifacts/` tree that two run records assert is committed.
**Every one of those was false.** `~/.claude/pipeline/pool.json`'s `source_repo` names
`~/Projects/openmrs/querystore`; that checkout is current with origin/main and
`pool-run`'s own `parity_problems(cfg)`, driven directly, returns 0 problems. The first checkout
was simply 3 commits behind (`e4df73f`, `bbd68fb`, `34845fd`). This is proposal P3's whole
evidence, and it is this pass's own near-miss rather than a record's.

---

## P1 — a claim-home sweep terminates by changing the KIND of question: enumerate the claim's SUBJECT, not its wordings

**Corroboration: 3 records, and one of them at 6 cycles. Bar (a) and bar (b).**

- `#374`:18 — "[harden c3,c4,c5,c8,c9,c10] Eighteen homes of a claim the change falsifies,
  found one per cycle by SAMPLING. The last two were pairs (a README paragraph and the javadoc
  summarising it) and one was at a site that never names the key. · cost: 6 cycles"
- `#374`:24 (Where a skill blocked) — "ten cycles each found exactly one more prose home by
  sampling an unbounded corpus; the loop could not terminate that way. What ended it was the
  skill's own 'change the KIND of question' rule: enumerating the population by SUBJECT (the
  corroboration answer, the chip's key set, the accessor's visibility) rather than by phrasing
  bounded it at 59 sentences, all checkable."
- `#379`:18-22 — four consecutive cycles each on one more home of one claim (c4 an unchanged
  neighbour, c5 "the same claim surviving in a SECOND home", c7 "'is not a fourth case' in two
  homes", c8 "the readings count surviving in the class javadoc four lines from the field, after
  I fixed only the ADR home"); and its Termination note at :28, "the cheaper move is to sweep a whole
  CLASS tree-wide the moment one instance is found".
- `#294`:137 (CONTINUATION r3) — "the unscoped regime-split claim had a FOURTH home round 2's
  three-site fix missed, in the javadoc of the very case whose CLAIM is the shape the measurement
  disclaims · blocking · cost: 1 round".

**This is `REJECTED.md`:1264-1275's reopen, met.** That entry parks "what makes two refuted
claims the same kind is their subject and property, not their wording" at 1 record with
**REOPEN ON:** *a run where differently-worded claims about ONE subject each took their own pass,
and with the rule stated on the subject alone.* #374 is that run: eighteen homes of **one** claim,
one per cycle, one of them worded without the claim's own key. The rule below is stated on the
subject alone.

**And it takes the three instructions `REJECTED.md`:2029-2032 left for whoever takes it next** —
cite `harden`:268, state the rule on the subject, and give an imperative that reaches a
behavioural check as well as a structural one.

**Edit (harden:390, `Don't stop correcting a claim at the site you noticed it`).** Replace the
clause *"; treat no list of those mechanisms as closed"* with:

> **When a sweep is still returning one more home per pass, the failing artifact is the sweep.**
> Do not widen it again — change the kind of question, as *What ends the loop is a change in the
> KIND of question* requires of guards. A token search enumerates HOMES and is unbounded; the
> claim's SUBJECT enumerates a population and is not. Name the subject — the answer it gives, the
> key set it ranges over, the visibility it asserts — list every sentence in the tree about that
> subject, and check each: resolve it where it names an identifier, RUN it where it asserts
> behaviour. On #374 six cycles each found one more home of one claim by sampling, one of them at
> a site that never names the key; enumerating by subject bounded the same claim at 59 sentences.

**Edit (pr-harden:717-718, `Correcting a claim means finding every home of it`).** Replace
*"; treat no list of those mechanisms as closed"* with a two-sentence form of the same rule,
because `REJECTED.md`:1359 verified the fixer has no route to harden's text and this ledger has
ruled two homes defensible for this pair:

> **A sweep that returns one more home per round is the failing artifact, not the edit.** Stop
> widening it: name the SUBJECT the claim is about, list every sentence in the tree about that
> subject, and check each — resolve the ones naming an identifier, RUN the ones asserting
> behaviour. A token search enumerates homes and does not terminate; a subject enumerates a
> population and does.

**Prunes:** the open-ended mechanism list in both files. "Treat no list of those mechanisms as
closed" is precisely the *another entry on the list* shape `harden`:268 forbids, and #374 supplies
what would have been the third entry ("a site that never names the key") — writing it down would
have been the anti-pattern. The #266 measurement that earned the two named mechanisms is kept
verbatim in both files; only the invitation to keep extending the list is retired, and #374's six
cycles are the measurement retiring it.

**Net: roughly flat** (one clause out, one rule in, per file).

---

## P2 — `pull/<n>/head` is not the pushed head, and the gate the skill ships will bill a round for the difference

**Corroboration: bar (c) — the skill instructs a fetch that can produce a state its own gate
script rejects. 1 record.**

- `#379`:29 — "pr-harden Step 1 — GitHub's `pull/<n>/head` ref LAGGED behind the pushed branch
  by one commit; `gh pr view --json headRefOid` and `git ls-remote` both had the new sha.
  Briefing the reviewer off the pull ref would have reviewed the wrong sha. The skill's 'the push
  had not landed when the fetch ran' guard names the symptom; the remedy is to cross-check
  headRefOid."

**Why clause (c) rather than a park at 1 record / 0 rounds.** `pr-harden`:125-126 instructs
"Fetch and review the **pushed** head, not the local worktree: `git fetch origin
'pull/<n>/head:pr-<n>-r<round>'`", asserting that ref IS the pushed head. `pr-harden`:140 already
denies it in the same document, listing "the push had not landed when the fetch ran" as a cause of
a stale sha — so both halves are in the file and the instruction never draws the conclusion. The
second half is a fact about the gate script, not an inference: `pr-harden-gate.sh`:350-354 compares
the last `reviewed_shas` entry against `git rev-parse HEAD` **in the worktree**, so a reviewer
briefed off a lagging pull ref that returns zero blocking findings is blocked by the skill's own
gate and owes a further round. Following the instruction can therefore wedge the run.

**Edit (pr-harden, immediately after the fetch line at :125-126).** Add:

> **That ref is GitHub's copy and can lag the branch you just pushed.** On #379 it was one commit
> behind while `gh pr view <n> --json headRefOid` and `git ls-remote origin <branch>` both had the
> new sha. Read `headRefOid` first and require the fetched sha to equal it. A reviewer briefed off
> the lagging ref reviews bytes that are not the head, and if it returns zero blocking findings the
> cycle gate compares what you recorded against this worktree's HEAD and owes you another round —
> so the round is lost either way.

**Prunes:** nothing outright; it supplies the missing remedy to a cause :140 already names, so
the "or the push had not landed when the fetch ran" clause stops being a dead end. **Net: +5
lines.** Justification: the case it catches costs a full review round against stale bytes, and the
existing text can only diagnose it after the round is spent.

---

## P3 — Step 6's own disambiguator names the stale checkout on this machine

**Corroboration: bar (c) — the document offers a test its own ledger records as non-discriminating,
and the consequence is a false report from the battery Step 6 prescribes. 1 banked measurement +
this pass's own near-miss.**

- `REJECTED.md`:1733-1746 (banked 2026-09-07) — "the source repo has TWO checkouts, and Step 6's
  battery proves nothing about the one the pipeline writes in... Two checkouts on this machine
  answer that test identically — `~/Projects/openmrs/openmrs-module-querystore` and
  `~/Projects/openmrs/querystore`, both `openmrs/openmrs-module-querystore.git`." Its remedy was
  applied to the **filesystem** (the pool's checkout was fast-forwarded); the sentence that sent
  the pass there was never touched, and `pool.json` was deliberately left as the authority —
  "naming which checkout is authoritative is that file's job".
- This pass: Step 6's battery, run against the checkout the skill's parenthetical names, reported
  four drifts that do not exist. Verified by driving `pool-run`'s own `parity_problems(cfg)`,
  which reads `pool.json` and returns 0.

**The banked entry's REOPEN ON is "a pool-run retro that commits on the stale base, or a third
checkout", and neither happened** — I am naming a different consequence of the same cause, not
claiming that reopen. The stale-base hazard is about what a retro *writes*; this is about what the
battery *reports*, and a false drift report is the more expensive of the two because a retro acts
on it.

**Edit (skill-retro Step 6, third bullet).** Replace *"(this pipeline's source is
`openmrs-module-querystore`; `git remote -v` in that checkout is the authority)"* with:

> (which checkout that is comes from `~/.claude/pipeline/pool.json`'s `source_repo` — the remote
> cannot answer it, because two checkouts of this repo exist on this machine and `git remote -v`
> is identical in both, one of them named for the repo and behind. Fetch it and confirm it is not
> behind origin before any `cmp`: against a stale checkout the whole battery below reports drift
> that does not exist, which this retro did, four times, before checking.)

**Prunes:** the `git remote -v` test, retired by the measurement at `REJECTED.md`:1733-1746 and by
this pass. **Net: +2 lines.**

---

## Observed, not yet actionable (parked, with counts)

- **Prose-correction cycles → ~33 records** (previous ~31 at :2373, plus #374 and #294's
  continuation). #374: "Ended by deleting the CLAIM SHAPE, not by a fourth attempt", cost gate
  pass 2 + cycles 6 and 9. #294:168: "FIVE successive drafts of one ADR section were refuted...
  I reached for that after the fourth refutation, not the second" — the shipped rule
  (`harden`:383-387) says *second*, so both are the rule applied late, which is the sharpening
  #338 and #310 already recorded. Remedy stays killed.
- **Rate-limit / session-limit agent death → 22 records** (previous 20 + 1 measurement at :2375,
  plus #374 "one Phase 2 agent died on a session rate limit (429). Retried with a leaner brief
  after the reset and it converged, matching the recorded pattern" — the 2026-08-31 P2a remedy
  working — and #379's "one session rate-limit interruption between harden cycle 8's pass and its
  retry").
- **resolve-ticket Step 3's "revise and re-run ONCE" is undefined for a plan WITHDRAWN and
  replaced — 1 record** (#379:27, cost 0, read as allowed). Not a contradiction, an ambiguity.
  **REOPEN ON:** a second record, or one where the reading cost a gate pass.
- **In a measurement/documentation PR the blocking findings are about CLAIMS, not code — 1
  record** (#294:177: "all 15 findings, and every one of the 6 blocking ones, were about CLAIMS.
  The production diff is comment-only and the test held up under four adversarial mutation
  attempts"). **REOPEN ON:** a second, with a remedy that is not "review the claims too".
- **A FINISH rule that costs something real — 1 record** (#294:162: the verifier's 3-line
  robustness observation is non-blocking, so FINISH sends it to a follow-up rather than into the
  cleared sha; "correct, but worth recording as the one place the rule costs something real").
  #374 and #379 both record FINISH working as intended with no cost. No change proposed.

## Shipped rules confirmed working (no change proposed)

- **pr-harden's cap raise on a shrinking-findings signal** (#294:111, :158-161): default 4 reached with 1
  blocking finding; raised to 5 on the skill's own stated signal (4/5/4/1, zero re-raises, round
  4's finding caused by round 3's fix); round 5 returned zero. "The raise was correct and the
  signal test is what made it decidable rather than a judgement call."
- **resolve-ticket's refutation gate** (#379): the gate named a discriminator, running it
  withdrew plan v1 entirely before any code was written.
- **pr-harden FINISH not editing the cleared sha** (#374, #379, #294 CONT).
- **harden's deletion-over-rewording rule** (#374, #294 CONT) — applied late in both, but it is
  what ended both loops.

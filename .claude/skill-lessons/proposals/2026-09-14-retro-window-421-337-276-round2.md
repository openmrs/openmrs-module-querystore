# Round 2 — revised after refutation. Window: #421, #337, #276 (2026-09-14)

Round 1's verdicts: P0 SOUND-with-a-blocking-revision, **P1 KILLED**, P2 REVISE, P3 REVISE, P4 REVISE,
plus one class the proposals MISSED that the refuter certified at the bar. Every round-1 citation I
re-checked held, including three figures of mine that were wrong. What follows is the revised set.

Round 2 exists because three of these changed SHAPE rather than merely shrinking: P1's surviving
residue, P2's promotion, and P5, which is new and therefore unrefuted.

---

## P0' — PRUNE. `pr-harden` State: delete the marker-decides-ownership paragraph (918-928).

**Unchanged in substance; the fold is corrected.** Round 1 confirmed the contradiction from the gate's
code, and I re-verified it: `UNATTENDED` is read at `pr-harden-gate.sh:268`, inside `if [ "$AWAITING"
-gt 0 ]` at `:264`, so with no fresh await the marker block cannot change the outcome — and the doomed
paragraph's own cited incident is a `phase: building` block, which never evaluates it. Ownership is
decided at `:203-217` off the ENTRY's `.owner`. Gate header `:174-179`: "ASK IT OF THE ENTRY, NEVER OF
THE UNATTENDED MARKER… it is a different question about a different file."

**Two corrections from round 1, both verified:**

1. **The fold must preserve the marker's TRUE clause, and not in the `owner` paragraph.** A live marker
   whose pid is not an ancestor leaves this session ATTENDED, so the awaiting-yield allows — pinned at
   `pr-harden/gate-test.sh:88` ("a co-located pool run must not make us unattended" → allow) and
   `harden/gate-test.sh:75` ("foreign marker, entry ours, awaiting → allow (we are attended)").
   Deleted wholesale, that behaviour is stated nowhere in the skill. It goes into the attendedness
   paragraph (893-900), where the marker already lives; putting it in the `owner` paragraph would
   recreate the conflation the deletion exists to remove.
2. **The doomed paragraph carries a SECOND stale claim** nobody had noticed: 926 says the state file is
   "still keyed on `$PWD` alone", while State's own opening at 817-818 says "keyed by the WORKING TREE's
   **physical** path — `pwd -P`, symlinks resolved, which is what both hooks key on". The gate agrees
   (`KEY="$(pwd -P)"`). Recorded in the commit note so it is not re-learned.
3. My "46 lines above" was 57. Not shipped text; corrected here.

**Bar:** Step 3 clause 3 — a document contradicting itself AND its own gate script, twice over.
**Prunes:** ~11 lines and two false claims; ~2 lines fold back.

---

## P1 — KILLED. Do not add a trigger for reaching the claim-shape deletion sooner.

Dropped. The citations settle it and I verified both:

- `REJECTED:2561-2563`, from the retro 11 hours before this one: *"It re-proposes a killed remedy.
  **:2138-2141** … A trigger UNIT is that remedy in different grammar."* My "on the SECOND such cycle,
  not the fifth" is a trigger unit. Third grammar, same remedy.
- `REJECTED:2564-2566` shows the pass-level form already counted at `:2281-2283` (#294/PR410): *"three
  consecutive Phase-2 passes each found false claims in prose the previous pass had just written … I
  reached for it late, after the fourth refutation, not the second."* That is my proposal, already
  parked, in the record's own words.
- `harden:386-387` already ships the threshold: *"Once a second attempt at a claim of some kind has been
  refuted, the move is to stop making a claim of that kind, not to make a better one."*
- And my justification overstated the evidence: #337 names Termination FAVOURABLY (`#337:22`), and
  `#276:21` is a test-arrangement finding at cycle 3, which `harden:209-210` refuses to count as
  documentation — so "five consecutive" and "converged at cycle 1" do not survive the findings list.

**Increments the parked count instead:** prose-correction cycles ~33 (`REJECTED:2680`) → **~35**.

### P1-R — the residue that survives: a ROUTING fix, no trigger and no count.

The refuter identified this and it is the only part of P1 that escapes `:2138-2141`. `harden:199-201`
sends the documentation-pass agent through *Don't stop correcting a claim at the site you noticed it*
"in full", and names **nothing** from *Don't rewrite prose faster than you verify it* — the rule both
runs needed. Name it there too, inside the existing first condition so the list stays at "three
things" and no count moves:

> …provided that agent does three things: the correction method in *Don't stop correcting a claim at
> the site you noticed it*, in full, **together with the deletion discipline in *Don't rewrite prose
> faster than you verify it***; the build; and, for any claim about behaviour, RUNS it rather than
> reads it.

**Why this is not P1 in a fourth grammar:** it states no timing, no unit and no threshold. It adds the
name of an existing rule at the one site a documentation cycle is standing. `REJECTED:2434-2436`
records routing as the shipped shape for the analogous case ("What shipped is therefore a ROUTING fix
— that rule lives in the guard-mutation bullet and is now named from the claim-correction bullet — not
a new rule").
**Refute this specifically.** +1 clause.

---

## P2' — `harden`: promote the KIND-of-question sentence out of the TEXT-guard bullet.

**Revised on all six round-1 objections.** The three blocking ones were right: the widenings span
**three** cycles (c2, c3, c4 — verified in the record), the "two questions" REPLACED the enumeration
rather than the reverse, and `#421:22` belongs to the claim-shape family, not here. Dropped from the
citation.

Objection 4 is the one that changes the shape. A disclaimer inside a bullet headed "And where the
guard is over TEXT" is unreachable by a reader standing at a production type predicate — which is
P1's own "the rule was not reached" argument, and I cannot accept it there and refuse it here. So
**promote**: the sentence becomes its own bullet immediately after the text-guard bullet, covering the
open-ended-widening family wherever it arises, with its existing #256 and #330 worked terminators
staying with it.

Its added scope sentence, with no claim about the remedy (objection 5):

> The widening family is not text guards alone: on #421 a production predicate over TYPES escaped
> three cycles running — exact type equality, then a raw type, then wildcard and type-variable bounds
> — each escape found by the next fresh agent.

And at `harden:396`, drop "of a guard" from the pointer, which objection 6 correctly says is a
substantive widening rather than a copy-edit: the pointer's target is now a general bullet, so the
narrowing it acknowledged is gone.

**Bar:** clause 2 — one record (#421), three cycles.
**Prunes:** the "of a guard" narrowing. Net +3 lines, and one rule becomes reachable from outside a
bullet about strings.

---

## P3' — `harden` 389: the write-side anchor. Two words fixed.

Sound in substance per round 1, which also verified that nothing in `REJECTED.md` touches the
mechanism and that `:2681-2684`'s named residue is a different limb. Both revisions taken:

> **Anchor the insertion after the preceding member's closing brace, never on the following member's
> signature** — which is what did it on #276, twice in one run, the second time stranding the
> canonical statement of a rule other javadoc points at.

- "a rule three `{@link}`s point at" → "a rule other javadoc points at". A count that tracks the code
  is the defect `harden:388` forbids, and it was load-bearing for nothing.
- "that is what does it" → "which is what did it on #276". One run's diagnosis of its own two
  instances; #234/#229/#255 are not characterized this way anywhere.

**On `REJECTED:1883-1887`'s standing "more text is not the remedy":** that ruling refused a fourth
*detection* remedy, and its reopen was consumed by the mechanical check. This is a write-side
prescription — where to anchor the edit — not another instruction to sweep afterwards. Stated here
rather than left unaddressed, as round 1 asked.

**Bar:** clause 1 on the family (#234, #229, #255, #276); the prescription has one record.
+3 lines.

---

## P4' — `harden` Termination: name the direction a reporting check must never fail in.

**Revised: the unsourced clause and the causal headline are gone.** Round 1 confirmed
non-subsumption on the right axis — `harden:242-250` and `pr-harden:293-306` are both about a TEST
guard's boolean; this is about a PRODUCTION reporter's two error directions.

> - **Where the thing you changed REPORTS — a warning, a finding, a check — name the direction it must
>   never fail in, and build the input for that direction.** Two runs, each found by a fresh agent
>   rather than by the author: on #337 a carve-out applied to both operands withdrew the record-side
>   exit, so an answer reproducing an operator note FAITHFULLY was reported, and the carve-out had to
>   become per-operand; on #276 a boundary asserted to "fail toward silence" did the opposite —
>   `statesWord` matched a fragment of a larger number and produced a false report on a published key.

- "on input the suite already had" — **deleted**; it is in neither record and was the clause carrying
  the non-subsumption, which now rests on the production-reporter axis instead.
- "because a fix that closes a miss moves the other one too" — **deleted**. It describes #337 only;
  `#276:9` is a direction claim that was false from the start, which is a different mechanism.
- The imperative is now `#276:17`'s own wording ("the direction the check must never fail in"), which
  both records support.

**Bar:** clause 1 only — two records, one event each, and round 1 is right that neither meets clause 2
(`#337:15` cost "found before PR"; `#276:9` cost 1 pass). Ships on two records or not at all.
+4 lines.

---

## P5 — NEW, from the class round 1 certified as missed. `harden` Phase 1: ask the INBOUND question.

`harden:32` asks of every documented statement near the slice: *"does my change make this statement
false?"* Nothing asks the opposite and cheaper question — **is the standing claim I am about to build
on true?** Three records paid for it, and in two the refutation was already sitting in the tree:

- `#421:8` — the TICKET's claim, carried into the plan, that a branch was unreachable: the fixture's
  own javadoc "already records a review agent measuring exactly that, and records that an earlier
  javadoc called it unreachable and was wrong". `#421:16` — "no lazy association is reachable from this
  loop" was false and "contradicted conceptUuid's javadoc 200 lines down" · 1 cycle.
- `#276:8` — a reachability bound "published in README, ADR and javadoc" that "Contradicted a figure
  already in the tree (129 multi-row substances)" · 1 harden pass.
- `#337:8`, `#337:11`, `#337:12` — two standing ADR Decision 61 claims and a published A/A control
  band, the band "published twice and refuted twice" · 2 harden passes.

Draft:

> - **And ask it INBOUND, which is cheaper: is the standing claim you are BUILDING on true?** The
>   thread above asks whether your change falsifies a documented statement; this asks whether the
>   statement was ever true. Three runs paid for it — a ticket's own claim that a branch was
>   unreachable (#421), a reachability bound published in a README, an ADR and a javadoc (#276), and two
>   standing ADR claims plus a control band published twice (#337, two passes) — and in two of the three
>   the refutation was already in the tree, in a javadoc 200 lines from the edit and in a figure the
>   repo already carried. So before a plan rests on a published claim, grep for its subject, and read a
>   published figure as a measurement whose date you do not know.

**Bar:** clause 1 (three records) and clause 2 (#337, two harden passes). The "refutation already in
the tree" sub-clause is stated at its true strength — two of three, not all.
**Placement:** Phase 1 "Trace outward", as a peer of *Invalidated invariants in unchanged neighbors*,
because it is the same population of statements read in the other direction.
**Refute this specifically** — it is new and nothing has attacked it.

---

## Budget

Added: P1-R +1 clause, P2' +3, P3' +3, P4' +4, P5 +7 ≈ **+18**.
Pruned: P0' ~−9 net, the "of a guard" narrowing, and P3's stale count ≈ **−10**.
Net ≈ **+8** across `harden` and `pr-harden`, against P1 killed outright. Justification: P0' removes
two false claims including one that re-proposes a pinned regression, P2' makes an existing rule
reachable rather than restating it, and P5 is the window's only genuinely new rule.

---

## Parked, with counts (unchanged from round 1 except where noted)

1. **Prose-correction cycles: ~33 → ~35 records.** #276 and #337. Remedy killed at `:2138-2141`;
   trigger units killed at `:2561-2563`. P1 above is the third grammar to die.
2. **A test pinned from one side only survives a TIGHTENING.** `harden:235`'s list is
   exhaustive-shaped ("deleted, its arms swapped, its comparison **loosened**, or rewritten");
   `#337:16` is `>=`→`==`, blocking, 1 round. **1 record.** Round 1 calls this the sharpest of the
   three parked items.
3. **A third mechanism by which a mutation loop silently runs nothing** (#421, macOS has no
   `timeout(1)`; the `||` fallback never fired because grep exits 0 on empty input). Family ruled at
   `harden:236-241`. **3 records for the family, 1 for the mechanism** — and the neighbouring rule says
   what ends such a loop is a change in the KIND of question, not another entry on the list.
4. **A logger positive control before reporting an absent WARN** (#421's verifier did it unprompted).
   **1 record, cost 0.**
5. **A measurement taken from an agent's report and never re-run by the author** — `#421:11`,
   "unguarded it reddens 10 of 13", 1 cycle. Sits in tension with `pr-harden:326-334` ("do not ask
   either agent to re-derive evidence its brief already carries"). **1 record**, and a rule-versus-record
   tension rather than a document self-contradiction, so clause 3 does not reach it. Raised by round 1.
6. **Rate-limit / session-limit agent death: 22 + 1 → 24 + 1 records.** #421 and #337 each stopped on a
   session usage limit mid-run and resumed from disk with nothing lost. Both confirm the dead-phase
   contract; no proposal.

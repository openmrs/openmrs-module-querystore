# Retro proposals — window 2026-09-23 21:33 → 2026-09-24 04:52 (10 records)

Records: `…-2026-09-23-…-494.md`, `-496.md`, `-498.md`, `-488-PR501.md` (mirrored unread by 846a225 and
278d42f), and `2026-09-24-…-433.md`, `-451.md`, `-469.md`, `-476.md`, `-477.md`, `-505.md`. The four
2026-09-23 runs started before 99594f1 (pr-harden 0.28.0 / resolve-ticket 0.18.1) and ran under
0.27.0 / 0.18.0; the six 2026-09-24 runs started after 278d42f (0.29.0 / 0.19.0). Launch mode, from
`~/.claude/pipeline/ledger.json`: #433 and #451 pool-launched (unattended); #469, #476, #477, #498,
#505 `launched_by: work`.

Status: refuted (one round; 0 killed, 4 revised, 1 as drafted — see REJECTED.md 2026-09-24). Transcript line numbers in this draft run one below the 1-indexed JSONL line; the ledger entry carries the corrected ones.

## P1 — spawn delegated agents with `run_in_background: false`; the paragraph saying the flag does not exist is stale

Bar: (c) — `pr-harden` :1011-1023 tells the orchestrator to collect in-turn by putting several `Agent`
calls in one message, and states the harness has no `run_in_background` flag (*"neither exists in the
harness as of 2026-09-20 — the `Agent` schema carries no such parameter"*). Followed as written, the
spawn takes the default, which is background, and the skill's own gate refuses the yield that follows
(`harden-cycle-gate.sh` / `pr-harden-gate.sh` unattended branch). One sighting: #433:22, cost two turns.

Measured 2026-09-24 in the transcripts (`Agent` tool_use inputs and their tool_results):
- #451 (`bacbdf7b…`, unattended): every `Agent` call passes `run_in_background: False`. Phase 2's four
  lenses spawned at lines 293-296 in one message; the four reports returned as results at 299-302,
  before any other tool call. No Stop block in the run.
- #433 (`dbfc9042…`, unattended): refuter at 139 with `False`, result in-turn at 140. Phase 2 wave 1 at
  390-396 omitted the parameter: each result is "Async agent launched successfully"; the turn ended and
  the Stop hooks blocked at 402/405 and again at 475/478 ("This run is UNATTENDED and you ended your
  turn with a background agent outstanding: phase2 x4"). Waves 2b (586-589) and 2c (618-621) and every
  later agent passed `False`: all results in-turn (592-595, 622-625), no further block.
- The `Agent` schema in this 2026-09-24 session carries `run_in_background` (default: background).

Edits:
1. `pr-harden` :1011-1023, the paragraph "**Collecting in the same turn means never polling
   afterwards.**" becomes:
   > **Collecting in the same turn means spawning in the foreground.** Pass `run_in_background: false`
   > on each `Agent` call: several such calls in ONE message still run concurrently, and each agent's
   > report comes back as that call's own result, in this turn. Measured 2026-09-24 on two unattended
   > runs: #451 spawned its four Phase 2 lenses so and had all four reports before its next tool call;
   > #433 spawned its first wave at the default, which is BACKGROUND, ended the turn with four agents
   > outstanding, and was refused by the unattended gate twice. *This paragraph said on 2026-09-20 that
   > no such parameter existed; the 2026-09-24 `Agent` schema carries it.* **A delegated agent's output
   > file is its whole JSONL transcript, never its report**: [the 2026-09-01 measurement, unchanged]. Where
   > you need to block on something that is NOT an agent — a build, a server coming up — that is a
   > background Bash task, whose output file is its stdout and is safe to read.
   Deletes: "and the report arrives by itself in the completion notification's `<result>`" (true of a
   background spawn only), the 2026-09-20 italic sentence, and "What outlived the tool is the
   transcript file, which is what this rule is about."
2. `harden` Phase 2 step 1: "Spawn four parallel review agents (…) over the current diff." gains
   ", in ONE message, each with `run_in_background: false` — the default is background, and an
   unattended run's gate refuses the yield that leaves them outstanding (#433)". This is the line #433
   was executing.
3. `resolve-ticket` :263: "…collect the agent inside the same turn." gains "— spawn it with
   `run_in_background: false`."
Prunes: 3 sentences in (1). Net ≈ +2 lines.

## P2 — wait on a background Bash task inside the turn, not by yielding for its notification

Bar: (a) 4 records, all attended (`launched_by: work`) runs mid-flight: #479:20, #489:18 (parked
2026-09-23 at 2 with REOPEN ON "a record where it cost more than a turn"), #498:20 ("Stop gate fired
twice while a background root build ran with no agent outstanding; resolved by a foreground bounded
wait"), #505:20 (twice; transcript 384-399: a background mutation-probe Bash, then a Monitor, each
followed by a yield the gate blocked with "resolve-ticket is mid-run and has not opened its pull
request yet"; recovered at 404 with a foreground loop). #498 and #505 each cost two turns, which meets
the park's reopen condition.

Edit: append to the P1 paragraph's last sentence:
> Wait on it inside the turn too, with a foreground loop bounded under the tool's ten-minute timeout —
> #505's `for i in $(seq 1 110); do grep -q 'files\? changed' $F && break; perl -e
> 'select(undef,undef,undef,5)'; done` — rather than ending the turn for its notification: the gate
> refused that yield on four runs mid-flight (#479, #489, #498, #505), twice on each of the last two,
> and each recovered this way.
Retires: the parked entry "Stop hook refusing a yield while a background build/capture runs".
Not edited, reported to the owner: `~/.claude/CLAUDE.md` "Wait on a condition" says "start long work
with `run_in_background`, or do adjacent work and let the completion notification wake you", which is
the yield the gate refuses mid-run. `pr-harden` :566-569 (the verifier's "hands the turn back at once
and notifies you") is the verifier's own section and is not touched: whether a subagent survives its
own yield was not measured. Net +5 lines.

## P3 — resolve-ticket Step 3 shows the refuter's await with `--only pr`

Bar: (a) — after 0.28.0's refusal-message fix, still one failed call each on #451:19 ("bare `await`
refused without --run … needed `--only pr` for the Step 3 refuter await") and #505:21 (same); before
it, #496:18 ("the resolve-ticket snippet at Step 3 omits `--only pr`") and #488-PR501:21. The only
`gate-state await` snippet in `resolve-ticket` is the harden one at :452 (`--run`, no `--only`); :258
points at `pr-harden`'s State for the rest.

Measured 2026-09-24 in a temp HOME on a `building` entry: `await "refuter"` → exit 2 (needs --run);
`await "refuter" --only pr` → exit 0, `awaiting: refuter (pr)`.

Edit: `resolve-ticket` :258 "The field and its snippet live in `pr-harden`'s **State** section." becomes
"The field lives in `pr-harden`'s **State** section. No harden entry exists yet, so this await is the
pr gate's alone:" followed by the two-line block (`await "refuter" --only pr` / `clear-await --only pr`).
Net +4 lines.

## P4 — `clear-await` takes no label; say so at the snippet

Bar: (a) 2 records, one failed call each: #480 (parked 2026-09-23, "confirmed gate-state:334") and
#494:18 ("`clear-await` takes no agent name and clears the whole list … cleared once after all four
returned. Cost: one failed call"). Measured 2026-09-24: `clear-await refuter --only pr` → exit 2,
"unrecognized arguments: refuter".

Edit: `pr-harden` :1146-1147 — the second copy of "Kept apart from the transition write above so that a
spawn never has to restate the phase." (it is also :1140) is deleted, and in its place: "`clear-await`
takes no label and empties the whole list: clear once, after the last agent of the wave has returned or
died." Net 0 lines.

## P5 — `pr-set --phase building` resets a prior PR's entry (gate-state + pool-test)

Bar: (c) — `resolve-ticket` :170 says the opening write is "`phase: "building"`, `round: 1`, no `pr`
yet", and the gate header (`pr-harden-gate.sh`:59) reads `building` as "has not opened its PR yet";
`gate-state pr-set` without `--pr` keeps whatever `pr` the entry held, with that PR's ledger. One
sighting: #477:26 — the entry after Step 1 carried `pr: 483`, `reviewed_shas`, `verified_shas`,
`declined` and `override_reason` from the ticket's earlier run (PR #483, same worktree); noticed at
Step 8 via `show`, cleared by hand (transcript line 1058).

Reproduced 2026-09-24 in a temp HOME: `pr-set --pr 483 … reviewed`, `reviewed-sha`, `verified-sha`,
`declined`, then `pr-set --ticket 477 --round 1 --phase building --blocking 0` → `show` prints
`"phase": "building", "pr": 483` with all three ledgers.

What it would have cost uncleared: `pr-harden` Step 0 (:72-80) refuses "a fresher entry claiming a live
round on another PR"; and a run dying before its PR has `pool-run`:1998 look the outcome up by the
gate entry's `pr`, which would be the merged #483.

Edit: in `pr-set`, a `--phase building` write without `--pr` is treated as a change of PR to none: the
existing drop (reviewed/declined/verified, with its print) runs when the prior `pr` is not null, and
`pr` is set to null. `pool-test.py` gains the case beside the handoff test (:885-898): a stale PR entry,
then the building write, asserts `pr` null and the three ledgers empty. `pr-harden` Step 0's sentence
"`pr-set` now drops `reviewed_shas` and `declined` when the PR number changes" gains "or when
`resolve-ticket`'s opening `building` write lands on an entry naming a PR (#477 inherited #483's)".
Guard, not diagnosis: why #477's earlier run left its entry uncleared was not established.

## Not proposed — observed, with counts
- harden Phase 2 run with fewer than four agents: 4 records (#479, #488, #491, #476:18, two agents,
  lenses paired). REOPEN ON unchanged (a merged-lens pass missing what a later fresh agent finds in
  that lens): #476's r1 raised nothing.
- A delegated refuter mutating production files while the orchestrator edits: 2 records (#485, #477:23,
  :28; cost a redo). The rule exists (`pr-harden` :1048 "DO NOT EDIT THE WORKTREE WHILE A…"); more
  prose for an unfollowed rule is the :1196-1198 class.
- Phase 2 escalating repeatedly on the run's own fixes: #477 (4 escalations, labelled override),
  #433 (2), and #482 (parked). The skill permits it and the override worked; no remedy identified.
- chartsearchai's nested `CLAUDE.md` byte budget (ProjectInstructionsGuardTest): 3 records (#433:10,
  #469:23, #477:29), 1-2 builds each. A property of the target repo, not of a skill.
- Usage-limit interruption: #476:16 (a Phase 2 agent died on a weekly-limit 429), #505 (transcript
  412, "You've hit your weekly limit", the record's "one account re-login mid-run"). 2 records, no
  convergence lost.
- 1 record each: an unfilled PLAN_PLACEHOLDER in the refuter brief (#469:21); the refuter's own doc-sweep
  list incomplete (#433:23); main took the ADR decision number mid-run (#477:30); pre-0.28 `--only`
  misplacement (#494:19, #498:19), which 0.28.0 addressed.

## 0.29.0's REOPEN ON conditions, read against its first six records
- Cost (REOPEN ON "a window of 0.29.0 records, read for rounds per run and for any 8h kill"): rounds
  #433 2, #451 1, #469 2, #476 1, #477 2, #505 2. Four of six paid the confirming blocking-only round;
  every confirming round returned 0 blocking. Durations from the ledger all under 2.5h; no kill. The
  cost is what 0.29.0 stated.
- Prose-only exception (REOPEN ON a clean full round's non-blocking findings reaching FINISH neither
  implemented nor declined): none — #451's r1 items were declined by the fixer or named the PR body;
  #505's body item went to FINISH as allowed; the rest were implemented.
- Fired twice / caps: no record shows either.
- No pipeline run filed an issue: `gh issue create` occurs 0 times in each of the six transcripts (3 in
  `f79f9941…`, the non-pipeline session that opened #512-#516, as the positive control); #505 itself is
  a consolidation issue opened 20:37Z by session `11df53f2…` (the owner-directed third pass of
  2026-09-23), merging six follow-ups.

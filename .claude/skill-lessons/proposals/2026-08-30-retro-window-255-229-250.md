# Retro window 2026-08-30 (second pass of the day) — #255, #229, #250

Records read (3, all written after `LAST` = 2026-08-30 10:42):
- `2026-08-30-chartsearchai-255.md` — #255 / PR 335, converged, 1 round / 3 harden cycles
- `2026-08-30-openmrs-module-chartsearchai-229.md` — #229 / PR 334, converged, 1 round / 2 harden cycles (pre-PR)
- `2026-08-30-openmrs-module-chartsearchai-250.md` — #250 / PR 333, converged, 2 rounds / 5 cycles

Linter: 10 files, 0 findings.

## P1 — `gate-state count_edits`: on a branch with no upstream, measure the commit half against the head the previous cycle recorded

**Bar: 2 records, plus clause 3 (a skill contradicting its own gate helper).**
- #255: "`gate-state --count-edits` reported `edits=0` for a cycle with 9 unpushed commits, because the
  branch had no upstream at that point."
- #229: "reported edits=0 with 3 unpushed commits on a branch that had no upstream yet. Did not affect
  the outcome (I measured commits myself) but the field was not reporting what its description says."
- Clause 3: `harden`'s State section says `--count-edits` counts "uncommitted lines plus commits not yet
  pushed, **both halves**, because a cycle that commits its work has still changed something", while
  `gate-state`'s own `count_edits` docstring says "a branch with no upstream has nothing to compare
  against, so that half is zero rather than an error". The gate blocks on `edits > 0` and allows on 0, so
  a cycle whose whole output was committed reads 0 and the gate permits the stop — fail-open in the one
  direction the gate exists to close. Pre-PR `harden`, which is where `resolve-ticket` Step 7 runs it, is
  the configuration that has no upstream.

**Edit (mechanical, in `~/.claude/pipeline/gate-state`):** `harden-set` records `head` (current HEAD sha)
on the entry it writes. `count_edits` keeps the `@{u}..HEAD` path unchanged when an upstream resolves;
when it does not, it counts `<previously recorded head>..HEAD`.

- Why not `git rev-list --count HEAD --not --remotes`: that counts every commit of the run, so on a
  pre-PR branch the number never returns to zero and the gate would block every stop until a push. The
  hook's own header says a gate that wedges turns is worse than one that lets an early stop through.
- Why not prose: last window recorded two prose remedies for `gate-state`'s `--only` defect that reached
  no caller, and the mechanical override that closed it.
- Residue, named: the first `harden-set` on an upstreamless branch has no previous head, so its commit
  half stays 0. This is better than today in the later cycles and no worse in the first.
- `head` is documented in prose only, not in the skill's state-file JSON example — the linter's
  `state_fields_vs_gate` check compares that example against `*gate*.sh`, and the reader of this field is
  `gate-state` itself, not the Stop hook.

**Prunes:** the docstring sentence "a branch with no upstream has nothing to compare against, so that
half is zero rather than an error" — retired by these two measurements, deleted rather than reworded.
Net: helper +6 lines, skill +1 clause.

## P2 — an insertion that separates a javadoc from the member it documents

**Bar: 3 records.**
- #234: "Inserting the SITE_* constants orphaned `LOCALLY_APPLIED_ATC_GROUPS`'s 43-line javadoc; and
  again later for `codesForThisSubstancesPresentations`. Twice, in the same slice, both silent."
- #255: "`findingTexts` inserted between `injectedFindings`' javadoc and the method, orphaning it — found
  independently by three agents" · cost 1 Phase-2 pass.
- #229: "Two orphaned javadocs from inserted methods — silent through compile, checkstyle and 1686
  tests" · cost 1 pass.

**Not covered by what is there.** `harden`'s "Invalidated invariants in unchanged neighbors",
`pr-review`'s "Unchanged neighbors" and `pr-harden`'s "Trace outward" all ask whether the change made a
neighbour's statement FALSE. An orphaned javadoc is still true — it has only stopped being attached to
its subject — so a truth check passes it, and #229 records compile, checkstyle and the suite passing too.

**Edit:** one bullet in `harden`'s anti-pattern about editing by script (the cause is an edit that
inserts a member): after inserting a member into an existing file, check that each javadoc block still
sits against the member it names.

**Prunes:** the stutter at `harden`'s gate paragraph — "so it can only ever add a cycle you owed — it can
only ever cost you the cycle you owed" — one of the two clauses goes. Net ~0.

## P3 — the mirror of "a mutation that did not take effect": a revert that did not reach the artifact

**Bar: the family is at 3 records; this DIRECTION is at 1, and I state that openly.**
- #250: "`api/target/classes` held a mutated class from a revert-check, and two probe runs silently
  measured the mutation. Caught only by an impossible answer."
- The family's other direction is already applied at `harden`'s "A mutation that reddened nothing has not
  shown the guard is dead until you know it RAN": #256 (nothing compiled, so nothing ran) and #263 (`zsh`
  did not word-split, so configurations ran unmutated).
- The existing bullet does not reach #250: there the probe answered, and the answer described a build the
  source no longer said. Its cost is two probe runs, not a cycle — so if the refuter reads the family
  framing as double-counting, park at 1 record rather than argue.

**Edit:** one clause on that existing bullet. **Prunes:** none; +2 lines.

## P4 — `pr-harden` step 6.2: the JDK example names 1.8 as the value

**Bar: 2 records, one repair attempt each.**
- #266: "the verifier's procedure anticipates a Java-21-vs-1.8 mismatch and suggests `java_home -v 1.8`;
  the actual failure was the OPPOSITE (`invalid target release: 11` under JDK 8, machine default 21).
  Cost: one repair attempt."
- #255: "this repo's pom targets Java 11 and that command resolves to the applet-plugin JRE on this box.
  The verifier lost an attempt to 'No compiler is provided in this environment' before finding
  temurin-11. The advice should be 'the JDK the pom targets', with 1.8 as an example rather than the
  value."

**History, stated so the refuter can check it:** killed 2026-08-27 as "the JDK rule names one direction
and hardcodes 1.8", on the ground that clause 3 was unavailable because the sentence states its own
antecedent ("a module on Java 1.8") and "the pom is a fact about a repository". That ruling is not
contested here. This is the count route, which that kill did not rule on and which #255 completes.

**Edit:** the parenthetical value becomes the pom's own target — read `maven.compiler.target`, then
`/usr/libexec/java_home -v <that>` — and #255's signature ("No compiler is provided in this environment"
= the resolved home is a JRE) joins the Mockito one already there. Net +1 line.

## Parked, not submitted

- **`gh issue view <n>` returning empty (exit 0) where `gh api repos/<o>/<r>/issues/<n>` works: 2
  records** (#236, and #255 "Every brief had to say so"). The bar is met by count, but this ledger has a
  standing ruling against writing a machine fact into a skill, and no form of the remedy avoids that.
  Recorded with the count so a third record meets a rule rather than a re-argument.
- **#255's own diagnosis that the mutate-and-restore hazard is written "for AGENTS' edits" while it bit
  the orchestrator's: checked and FALSE.** `harden` Phase 2 says "and your own measurement probes too";
  `pr-harden` says "before your OWN measurement probe" and "The axis is the FILE's state, not who typed
  the command". The rule existed and was not followed — the shape this ledger has already ruled is not a
  skill gap. No proposal.
- **#229's note on `harden` Termination is positive evidence, recorded as such:** "every one of the last
  seven passes found exactly one real defect, all prose, each in a file the change had not edited. Six of
  them would have shipped under any 'it's basically converged' stop." Any future proposal to weaken the
  confirming cycle has to pass this.

---

## Outcome (after the refutation gate)

- **P1 — APPLIED, revised.** `gate-state` now consumes a recorded head only when the same `owner` wrote
  it at an earlier `cycle`; reports an unmeasured commit half (no earlier head, or a head that no longer
  resolves) instead of returning it as zero; `harden`'s State sentence carries the no-upstream reading.
  Four checks added to `pool-test.py`; 289 pass, 0 fail. The submission had also under-cited its own
  support — this ledger named the same defect at this site on 2026-08-24.
- **P2 — APPLIED, re-grounded.** The "still true, so a truth check passes it" ground was cut as
  unmeasured (read against the member it now sits on, the javadoc is false — which is what harden's
  neighbour rule already asks, and all three sightings were found by a review pass). Shipped as cheaper
  prevention at edit time, with the subsumption stated: the bullet's existing clause is deletion-scoped,
  this one insertion-scoped.
- **P3 — PARKED at 1 record,** as submitted-with-its-own-caveat. Banked: the rule the record quotes,
  `pr-harden:"After any mutation rebuild with clean"`, exists in no skill.
- **P4 — APPLIED, revised.** 1.8 kept with its own antecedent (the earlier kill forbids pruning it),
  both recorded signatures added beside it, and applied at both homes, `verify-frontend-change`
  included.
- **Wording cut by the gate:** "never returns to zero", "block every stop", "an orphaned javadoc is
  still true", "no form of the remedy avoids that".

# skill-retro proposals — window 2026-08-28 .. 2026-08-30

Records read (5): 2026-08-28-...-296.md, 2026-08-29-...-238.md, 2026-08-29-...-256.md,
2026-08-30-chartsearchai-263.md, 2026-08-30-...-330.md, all in ~/.claude/skill-lessons/.
Linter: 10 files, 0 findings.

---

## P1 · pr-harden (patch) — name which `gate-state` subcommands accept `--only`

**Edit.** In pr-harden/SKILL.md, after the sentence at :779 ("Drop `--only pr` and it writes both gates
at once, which is what a nested `/harden` cycle needs."), append:

> `--only` is a flag of `await`, `clear-await` and `clear` alone; `pr-set`, `declined` and
> `reviewed-sha` write the `pr` entry by construction and reject it.

**Corroboration — clause 1 (four records).** REJECTED.md:829-832 parks this at 1 record (#293).
This window adds three: 256 ("`gate-state ... --only pr` is documented but the installed helper rejects
`--only` ... `reviewed-sha` had to be re-run without it"), 263 ("`reviewed-sha` rejects `--only pr`,
unlike `await`/`clear-await`. Cost one retry"), 330 ("`--only pr` is documented in pr-harden's State
section but the helper rejects it (`unrecognized arguments`)").

**What it prunes / retires.** The parked entry's own candidate remedy — "one line in the usage block
saying they are pr-scoped" — was APPLIED at the 2026-08-28 retro (REJECTED.md:863, "P2 (`gate-state`
usage block)"), and `~/.claude/pipeline/gate-state`:36-37 now carries it. All three new sightings are
dated after that. This proposal replaces that remedy rather than adding to it.

**Facts a refuter should check.** `gate-state`:167, :170, :185 add `--only` to `await`, `clear-await`,
`clear`. :146-163, :172-179 add it to none of `pr-set`, `harden-set`, `declined`, `reviewed-sha`.
Records 256 and 263 both say the flag exists on `await`/`clear-await` only; `clear` also has it, so
the proposed wording differs from the records on that point deliberately.

---

## P2 · pr-harden FINISH (minor) — re-derive an identifier allocated from a sequence `main` also appends to

**Edit.** In pr-harden/SKILL.md §7 FINISH, beside "Re-derive the PR description against the merging
head before marking ready", add:

> **And re-derive any identifier this branch allocated from a sequence `main` also appends to.** An ADR
> decision number is the observed instance: the branch takes the next free one when it writes the entry,
> and an upstream PR merged since can have taken the same one, which no build or test observes. Re-check
> it after every fetch or merge of `origin/main` and again here, and when it moves, sweep the citations
> of the old value — they live in javadoc and test names as well as the ADR file. Search for the number,
> not for a phrasing you used.

**Corroboration — clause 1 (three records this window) and clause 2 (#256 alone cost 1 pass + 1 round).**
296 [r1] blocking, 1 round: "main moved to ff2c7d48 where #297 already defines a DIFFERENT Decision 51,
and three of this PR's citations would have pointed at it". 238: "Both merges of origin/main landed an
upstream ADR entry on the number this branch was using (49, then 52). Renumbering is now twice-repeated
manual work"; and [r1] blocking, 1 round, "#236's ADR entry took the number this branch used". 256
[harden p7] 1 pass: "ADR numbering collided with origin/main's Decision 52 (#296)"; [r3] 1 round: "main
advanced again mid-run ... and took Decision 53, the number this branch had used — the second ADR-number
collision of one run".

**Guard, not diagnosis.** Nothing here claims why `main` moves or how often. The check's cost does not
depend on that.

**Related parked entry.** REJECTED.md:117/:275/:346/:410 park "`main` moving under a run: 1 record
(#284, a 16-conflict manual merge at PR time)". This proposal is narrower than that family — it does
not propose anything about conflicts or merge timing.

**Prunes.** Nothing. Net growth ~6 lines.

---

## P3 · harden (minor) — when successive deletions keep buying cycles, delete the claim SHAPE

**Edit.** In harden/SKILL.md, append to the anti-pattern at :309 ("Don't rewrite prose faster than you
verify it"):

> **And when deleting the clause is itself what keeps buying cycles, delete the claim SHAPE.** Four
> successive rules for one count were each measured false in turn on the #330 run, every replacement
> written under this rule; what ended it was publishing the measurement with its arrangement and no rule
> at all. If two attempts at a claim of some kind have both been refuted, the next edit is to stop making
> a claim of that kind, not to make a better one.

**Corroboration — clause 2 (#330 alone, 10 cycles) and clause 1 (three records).** 330: "cycles 5-15
were all one ADR section's prose, each cycle finding one real defect in the previous cycle's rewrite.
The rule ... is right and I applied it repeatedly; what finally ended it was deleting the CLAIM SHAPE
(any rule for a count) rather than the claim. Ten cycles." And in its Refuted list: "FOUR successive
rules for one count, each measured false. Ended only by publishing the measurement with its arrangement
and no rule." 263: "the four contributor magnitudes ... cost: 2 harden passes (a wrong correction, then
its correction)". 238 Shape worth noting: "The round that broke the pattern was the one briefed to
DELETE rather than rewrite."

**Not a re-proposal of a killed remedy.** REJECTED.md:32-40 killed "harden Termination: classify a cycle
whose only edits correct the previous cycle's prose" — a TERMINATION classification, which this is not.
The parked family entry (REJECTED.md:950, "Prose-correction cycles: 12 records") records the remedy as
killed and "deletion-over-rewording is the shipped rule"; this proposal extends that shipped rule rather
than replacing it.

**Prunes.** Nothing. Net growth ~5 lines.

---

## P4 · harden (minor) — what ends a guard-relocation loop is a change of question type

**Edit.** In harden/SKILL.md, append to the bullet at :214-220 ("And where the guard is over TEXT,
mutate the SUBJECT too"), after "treat no list of relocations as closed":

> **What ends the loop is a change in the KIND of question, not another relocation added to the list.**
> On #256 five evasions in turn each passed with the whole suite green — a differently-typed field, a
> parenthesised initialiser, an annotation prefix, a method reference, a call shape — and it settled only
> on switching from matching text to asking `getDeclaredFields`. On #330 the same happened over seven
> relocations and settled on stating the property positively, at class scope, instead of forbidding
> spellings. So after the second evasion, stop widening the needle and ask what question admits no
> relocation.

**Corroboration — clause 1 (two records) and clause 2 (each cost ≥2 passes; #256 cost 5).** 256
[harden p2-p6]: "FIVE successive evasions of the structural guards, each demonstrated with the whole
suite green, each fix opening the next ... Settled only by switching question-type — `getDeclaredFields`
for the field budget, name-over-bodies for the resolvers · cost: 5 passes". 330 [harden c2-c3]: "Guards
defeated by successive relocations, each with the whole build green: bare assignment, qualified
assignment, two line wraps, a getter read, extract-a-helper on the alias scan, extract-a-helper on the
token, a qualified receiver inside a helper. Ended only by stating the property POSITIVELY and at class
scope".

**Related applied entry.** REJECTED.md:706 records "A text guard defeated by relocating its SUBJECT: 1
record, 4 cycles + 1 round (#315) — APPLIED", which is the bullet this appends to. The applied text
warns that no list of relocations is closed; it does not say what terminates the loop.

**Prunes.** Nothing. Net growth ~7 lines.

---

## P5 · harden (minor) — a mutation that produced no red proves nothing until it is shown to have been in effect

**Edit.** In harden/SKILL.md, append to the evidence bullet at :202-203:

> **And a mutation that reddened nothing has two readings — the guard is dead, or the mutation never
> ran.** Read the build output, not the test count: on #256 a mutation check reported zero red because
> the mutated method no longer took the argument the edit used, so nothing compiled and nothing ran; in a
> compiling form it reddened two cases. On #263 three mutation configurations ran unmutated because `zsh`
> does not word-split an unquoted variable, and it surfaced only because a figure that should have moved
> did not. Prove the mutated form is what was built before concluding anything from its silence.

**Corroboration — clause 1 (two records).** 256, in its own words: "'a mutation that leaves the suite
green shows the guard is dead' -> not when the mutation fails to COMPILE ... Check the build output, not
the test count." 263: "`zsh` does not word-split unquoted `$vars`, so a loop passing multi-flag `-D`
strings to maven silently sent one malformed argument and three mutation configurations ran unmutated.
Detected only because a figure that should have moved did not. Cost ~20 minutes and one re-run."

**Prunes.** Nothing. Net growth ~6 lines.

---

## P6 · resolve-ticket Step 1 (patch) — pre-flight that the tree BUILDS, not only that a standalone exists

**Edit.** In resolve-ticket/SKILL.md Step 1, in the "Pre-flight the verifier here, not at the end"
paragraph, add:

> Pre-flight the **checkout** in the same breath: run the project's test-compile once before planning.
> The pre-flight covers the standalone and not the tree it will build, and a tree that cannot compile its
> tests fails in a way that reads as a code defect for several cycles before anyone suspects the
> environment.

**Corroboration — clause 1 (two records) and clause 2 (#238 cost ~3 build cycles).** 238: "the worktree
path contained `https:` ... javac splits its classpath on `:` — main compiled, every test failed with
'cannot find symbol' ... Cost ~3 build cycles before diagnosis. resolve-ticket Step 1's pre-flight checks
the standalone but not that the tree can build." 296: "the worktree this run was given was named from the
URL argument, so its path contained a COLON ... the build failed on a pristine checkout ... A sibling
worktree for issue 238 has the same defect."

**Honest caveat the refuter should weigh.** The specific cause is already fixed mechanically:
`~/.claude/pipeline/pool-run`'s `ticket_id()` normaliser (:1210-1235) now reduces a URL to its number,
and its docstring records this exact measurement. So this proposal is a guard over a class whose only
observed member is closed. Its remaining value is that `ticket_id` returns an unrecognised token verbatim,
and that the guard does not depend on the cause. If that is judged too thin, park it.

**Prunes.** Nothing. Net growth ~4 lines.

---

## Observed, not yet actionable (parked)

- **A fixer brief that asks an agent to reproduce evidence it was already given is a stall risk** — 1
  record (238, round 5's fixer hit the 600s watchdog re-measuring; a retry saying "do not re-measure,
  edit only" finished in 3 minutes).
- **A model override is the "change something between attempts" that works after a 429** — 1 record for
  the lever (238); the rate-limit death itself is 2 records (238, 296). pr-harden's retry contract names
  brief changes and not the model.
- **Four parallel Phase 2 agents must be told never to `mvn install`** — 1 record (256: "four agents
  installing into one shared slot-1 repo is the stale-api-jar trap by another route").
- **A single-word correction still buys a full confirming cycle** — 2 records (256 "three cycles ended on
  one edit each", 330 cycles 5-15). No remedy proposed: REJECTED.md:32-40 killed the termination-
  classification remedy for this on a walk-forward, and P3 is the tactic answer.

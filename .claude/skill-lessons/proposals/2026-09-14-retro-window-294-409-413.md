# skill-retro 2026-09-14 — five proposals for refutation

Window: three run records, all written 2026-09-13/14, none previously mirrored:
- `~/.claude/skill-lessons/2026-09-13-openmrs-module-chartsearchai-294.md`  (resolve-ticket + pr-harden, PR 416)
- `~/.claude/skill-lessons/2026-09-13-openmrs-module-chartsearchai-409.md`  (resolve-ticket + harden + pr-harden, PR 417, plus a merge-round addendum)
- `~/.claude/skill-lessons/2026-09-13-openmrs-module-chartsearchai-413.md`  (resolve-ticket + pr-harden, PR 419)

Older records and the killed-proposal ledger are in the same directory (`REJECTED.md`).
Skill files under `~/.claude/skills/`.

---

## P-1 · `harden` line 383 — the prose-correction trigger's UNIT

CURRENT (start of the bullet "Don't rewrite prose faster than you verify it"):
> When a cycle's findings are all in text *you wrote in the previous cycle* — a comment, a failure
> message, a doc paragraph — stop rewriting and change tactics: delete the unsupported clause …

PROPOSED: change the trigger's unit from a cycle to a pass or a cycle, and the authorship from
"you … in the previous cycle" to "an earlier pass of this run".

Citations offered: #294 ("cycles: 2 (harden, Phase 2 ran 5 passes in cycle 1)"); #409 ("five
Phase-2 passes in a row each found only prose defects written by the previous pass … The rule
works; it took four passes to reach for it"); #413 ("the four-lens pass ran five times and the
last two found only prose and text-guard evasion"). Parked entry at REJECTED.md:33-39, count 2.
Claimed bar: (a) two-or-more records.
Net growth: ~0 words. Nothing pruned.

## P-2 · `harden` line 389 — a mechanical trigger for the orphaned-javadoc sweep

CURRENT (inside "Don't trust a script's report that it edited something"):
> **That clause is about a DELETION; an INSERTION has its own silent failure — it separates a
> javadoc from the member it documents.** After inserting a member into an existing file, read the
> neighbours of the insertion point and check that each doc comment still sits against the member
> it names. Three runs paid a review pass each for one: #234 … #229 … #255 … It is cheap while you
> still hold the file open, and otherwise it is found by whoever reads the class next.

PROPOSED: append a command that locates the insertion points from the diff, say a hit is a place to
read rather than a finding, and DELETE the closing sentence ("It is cheap while you still hold the
file open, and otherwise it is found by whoever reads the class next.").

    git diff -U1 -- '*.java' | awk '/^\+\+\+ /{f=substr($0,7)} /^ /{p=$0} /^\+/{if (p ~ /\*\/[[:space:]]*$/) print f; p=""} /^-/{p=""}'

Citations offered: REJECTED.md:1883-1887 parks this lesson at 3 records and states its own reopen
condition — "**REOPEN ON:** a mechanical check that could catch it, or a record where the rule was
followed and missed one anyway." #294 records "[harden p2] ORPHANED JAVADOC created by the pass
that swept for orphans (third recurrence in this project)"; #409 records "[harden P2/1] The
inserted ArchitectureGuard method orphaned its neighbour's javadoc — reported independently by
three of four lenses".
Measurement of the command, run today in `~/Projects/openmrs/chartsearchai` (you can re-run it):
- fires on `4099f6d9` — the commit whose successor `278f474f` is titled "a javadoc orphaned by the
  previous pass" — one line, naming `CodesOnlyActiveOrderGroundingContextTest.java`;
- fires on `9e9df1e8` — whose successor `ca22df0c` says "the new architecture guard no longer
  orphans its neighbour's javadoc (three lenses reported it)" — naming `ArchitectureGuardTest.java`;
- silent on `278f474f`;
- over `git log -40 main` it printed 2 lines total;
- it ALSO fires on `ca22df0c`, the commit that REPAIRED the second orphan.
Claimed bar: the ledger's own stated reopen, both clauses.
Net growth: +~4 lines of prose and a 1-line command; −1 sentence.

## P-3 · `pr-harden` Step 4 (FIX, line 254) — which worktree the fixer runs in

PROPOSED: add one sentence saying the fixer works in the orchestrator's worktree and must not be
spawned with `isolation: "worktree"`, naming the three instructions that presuppose a shared tree
(the snapshot-and-compare hash in this step; restore-before-reporting; COMMIT's one commit per
round taken by the orchestrator), and noting that `harden` Phase 2 prescribes isolation for ITS
agents because those are concurrent reviewers.

Citation offered: #413 — "pr-harden:FIX — the fixer was spawned with isolation:\"worktree\", so its
edits landed in its own checkout rather than the loop's; the skill says \"leave your work
uncommitted in the worktree; the orchestrator commits\", which assumes a shared tree. Recovered by
generating a patch from the agent's worktree and applying it. Worth stating in the skill which of
the two shapes the fixer runs in."
Claimed bar: (c) — the document's own procedure is coherent under only one value of a parameter it
never states. One record.
Net growth: +1 sentence.

## P-4 · `pr-harden` VERIFY step 4 (line ~416) — the standalone is a process TREE

CURRENT step 4 says to stop the standalone you resolved, "running or not", and Bounds says "Kill
whatever you need to". Neither says the launcher is not the server.

PROPOSED: add a clause — `java -jar openmrs-standalone.jar` forks a child JVM that is the actual
Tomcat and also runs the bundled `mariadbd`, so kill the tree (or by port) and confirm the port
answers nothing before relaunching.

Citations offered: #409 verifier — "`java -jar openmrs-standalone.jar -commandline` forks a CHILD
JVM that is the actual Tomcat, so killing the launcher pid leaves the old server holding the port
and a readiness probe answers from it. The verifier caught that its first measurements came from
the pre-existing server and re-took them all after killing the whole tree." #308 — "**The
verifier's stray database process.** Killing the standalone's app and launcher left its `mariadbd`
holding the datadir lock … Cost roughly twenty minutes across three launch attempts and one
misdiagnosis."
Claimed bar: (a) two records.
Net growth: +2-3 sentences.

## P-5 · `resolve-ticket` line 100 — never spell `$MAVEN_ARGS` onto a command line

CURRENT: "`$MAVEN_ARGS` is read by `mvn` itself, so a plain `mvn -o clean install` already picks it
up: do not strip it, do not add `-Dmaven.repo.local` of your own, and do not be surprised that …"

PROPOSED: add "and do not spell it onto the command line" with the mechanism — `zsh` does not
word-split an unquoted parameter, so `mvn $MAVEN_ARGS …` passes the whole string as one argument.

Citations offered: #409 verifier — "`$MAVEN_ARGS` passed unquoted on a `mvn` command line is NOT
word-split by zsh, so the build installed into a junk repo directory named after the whole string.
The orchestrator never hit this because plain `mvn` reads the variable from the environment itself;
an agent that echoes it onto the command line does. Same shape as the #263 record." #263 —
"Repeated: `zsh` does not word-split unquoted `$vars`, so a loop passing multi-flag `-D` strings to
maven silently sent one malformed argument and three mutation configurations ran unmutated."
The mechanism already appears in `harden`:240 (for mutation checks).
Re-measured today: `zsh -c 'V="-Dmaven.repo.local=/tmp/head -o"; set -- $V; echo $#'` → 1; the same
in `bash` → 2.
Claimed bar: (a) two records of the mechanism.
Relevant standing ruling to check against: REJECTED.md:1146-1151 and :1826 — a MACHINE fact should
not be written into a skill; the prescribed route is a fact about the DOCUMENT.
Net growth: +1 clause.

---

## P-6 · `pr-harden`, the dead-phase / 429 contract (~line 986) — define what "a leaner brief" means
(submitted to the same gate as a supplement, after `REJECTED.md`:1071-1072 was found parked at 1 record)

CURRENT: after a 429 the skill names "a leaner brief and WAITING" as the remaining levers and never
says what to make leaner.

PROPOSED: one clause defining it — a leaner brief is one that does not ask the agent to re-derive
evidence the brief already carries; point its budget at the claims it doubts.

Citations offered: `#294`:28 ("the leaner brief also told the reviewer to spend budget on claims it
DOUBTED rather than re-confirming documented mutations — the first attempt had exhausted itself
re-running them"); `#238`:28 ("round 5's fixer stalled (600s watchdog) building a temp fixture to
RE-measure something the reviewer had already measured … Retrying with 'do not re-measure, edit only'
succeeded in 3 minutes"); `REJECTED.md`:1071-1072 parks it at 1 record.
Claimed bar: (a) two records.

# 2026-09-25 window — #514/PR524 second pr-harden loop, #515/PR540

Records: `2026-09-24-openmrs-module-chartsearchai-514.md` (second header, line 42, appended after the
fifth window counted the first) and `2026-09-25-openmrs-module-chartsearchai-515.md`.

## P1 — pr-harden: a declined BLOCKING finding ends the run; make that mechanical

Bar (b): one record, and the run went on for five more rounds (r2..r6) after the decline.

Evidence. #514/PR524 second loop, record :57 (`[r1-3] PR does not resolve #514 case 1 · blocking ·
declined`), :65 ("pr-harden:Termination: a declined BLOCKING finding 'ends the run as did not
converge', yet the run kept going … Nothing in the skill says whether to continue after a blocking
decline; I continued."). Transcript `31e8eac3-…jsonl`: line 178 is the only mention of r1-3 before the
end ("declined r1-3 with a reason on the record. Next, a fresh verifier runs") — no deliberation. Line
453's override line names only the round cap, not the declined blocker.

Skill text: `pr-harden/SKILL.md` :889-899 ("Two things end a run early … A declined blocking finding is
one of them … the run ends visibly as did not converge"), :1258 ("a declined blocker ends the run as
did not converge"), :436 ("A declined **blocking** finding does not end the loop quietly — see
**Termination**").

Edit, two options:
- (a) prose: :436 becomes "A declined **blocking** finding ends the run as *did not converge* — see
  **Termination**." ("does not end the loop quietly" can be read as "does not end the loop").
- (b) mechanical: `gate-state declined` takes `--blocking`; `gate-state pr-set --round N` refuses when
  a blocking decline from a round < N is on record and `override` is false.

Prunes: (a) replaces the sentence in place, net 0 lines. (b) adds gate code and a line in pr-harden.

## Parked in the draft (below the bar, for the refuter to check the count)

- `closingIssuesReferences` read 5s after `gh pr edit` returned `[515]` (transcript `1d4126e7-…` line
  1109, 01:18:44Z); the r2 reviewer's GraphQL read 11 minutes later returned `[513, 515]` (line 1113,
  01:30:02Z). The check `resolve-ticket`:520 prescribes ran and read green. 1 record, 1 round. Cause
  not established.
- Fixer ended its turn waiting for a build under pr-harden 0.32.0, brief carrying the in-turn rule
  (#514-2 :66). 1 post-0.32.0 record, cost a SendMessage resume.
- Counts advanced: Phase 2 escalations (+#515), prose made false (+#515), CLAUDE.md byte budget
  (+#515), main moving under a run (+#514-2), Phase 2 merged lenses (+#515), fix-introduced blocker
  (+#514-2, r2-2 → r3-1 and r5 → r6).

## The gate's reply (fresh read-only agent `aae7db165df66bc2c`)

- **P1(a) KILLED.** The record's "Nothing in the skill says" (:65) is false against :889-898 and :1258:
  a rule that exists and was skipped, the shape ledger :1885 and :4211-4214 kill as prose. The record
  never cites :436, and the run's stated reason was to get round 1's fixes reviewed, not a misreading.
  The transcript's thinking at lines 164 and 177 is redacted, so "no deliberation" is unsupported.
- **P1(b) KILLED.** `pr-set` rewrites `override` on every call (`gate-state`:388-389), and
  `override: true` allows any stop (`pr-harden-gate.sh`:99-100). So (b) either ends the run on r4-1 and
  r5-1 unfixed, or forces `--override` on every later round, disarming the Stop gate there.
- **Bar (b) not met.** Round 2 reviewed round 1's fixes, 0 blocking (transcript :206). r4-1 and r5-1
  were real blockers, fixed, with the verifier reporting works each time. Plausible cost is only r3-1
  (the #465 exit-test pattern, :297-299) and the open r6-1 (the cap raises, :904-913).
- **Parked list:** r5 → r6 is an incomplete fix of a pre-existing blocker (transcript :375), not a
  fix-introduced one. Only r2-2 → r3-1 counts.
- Citations corrected in place: :67 → :65, :68 → :66.

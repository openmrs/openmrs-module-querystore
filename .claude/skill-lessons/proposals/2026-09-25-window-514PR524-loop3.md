# 2026-09-25 second window — #514/PR524 third pr-harden loop

Records: `2026-09-25-openmrs-module-chartsearchai-514.md` (one header, line 1; "#514-3" below). Read
beside it, for the adoption question only: `2026-09-24-openmrs-module-chartsearchai-514.md` line 1
("#514-1", the resolve-ticket run with its nested pr-harden rounds) and line 42 ("#514-2", the second
loop), both counted by earlier windows (REJECTED.md:4100, :4379).

**#514-3's `transcript:` line is wrong.** It names `31e8eac3-…` (#514-2's session). That file's mtime
is 2026-09-25 02:30, its last lines are #514-2's record write (:450) and final report (:453), and it
holds no write of `2026-09-25-…-514.md`. The third loop ran in
`~/.claude/projects/-Users-danielkayiwa--claude-pipeline-worktrees-openmrs-openmrs-module-chartsearchai-514/7b912efd-d6be-4834-b016-81f5d6336c35.jsonl`
(292 lines, 05:26 to 10:30; its :285 appends the record). Mechanism, from that transcript :281-282: the
run looked its transcript up with `ls <dir>/*.jsonl -t | head -1`; BSD `ls` read `-t` as a file operand
("ls: -t: No such file or directory"), listed alphabetically, and `head -1` took `31e8eac3…`.
`pr-harden`:1247-1249 prescribes the uuid from the scratchpad path instead. Every transcript citation
below is to `7b912efd…` ("T3") or `31e8eac3…` ("T2").

## P1 — skill-retro Step 1: the record key "header's PR" drops a later run on the same PR

Bar (c). `skill-retro`:32-35: "several where a later run on the same ticket and date appended to it …
so count only the records whose header's PR the ledger does not already name". `skill-retro`:41: "Read
every record since the last retro". When the later run is on the SAME PR, the first sentence's key
excludes a record the second requires.

Evidence that the case is real, not hypothetical: REJECTED.md:4100 (09-24 fifth window) names
"#514/PR524"; REJECTED.md:4379 (09-25 window) counted "#514/PR524's second `pr-harden` loop" anyway. This
window's only record is the third loop on the same PR (#514-3:1). The `transcript:` line cannot be the
key either: #514-3's names #514-2's session (above).

Edit (`skill-retro`:33-35):

    `LAST` can carry records an earlier window already counted, so count only the records the ledger does
    not already name, and name each by its file and header line rather than its PR, which several runs can
    share (#514/PR524 carries three). Each is written by the skill that ran

Prunes: replaces the PR key in place. Net +1 line. Patch bump.

## P2 — pr-harden Step 0: a PR the pool hands back arrives with no entry, so carry forward from its run records

Bar (a): two records, **both on #514/PR524** (two runs of one PR, weaker independence than two tickets).

The mechanism. `pr-harden`:71-73: a same-PR entry is adopted, "carry its `round`, `declined` and
`reviewed_shas` forward". `ticket-pool`:49-51: a ticket whose run ended `draft` is handed to
`pr-harden` by the next invocation. `pool-run` clears the gate entry at every worktree it creates or
removes: `claim_slot` :1141-1146, `release_claim` :1193-1201, the driven path :3235-3243 (a resumed
session, :3230-3233, keeps it). So on that path Step 0's carry-forward has nothing to read.

- **#514-2** (`2026-09-24-…-514.md`:42-73). Started with no entry: T2:32 writes `--round 1 --phase init`,
  and T2:144 reads `reviewed … (1 round(s))`. It knew four rounds had run: T2:149 prints the PR body
  ("Four fresh-reviewer rounds ran"), and #514-1:3 records `rounds: 4`. Its round 1 and 2 briefs carry no
  blocking-only instruction (T2:134, :195); round 2 recorded 0 blocking and a round-2 fixer ran
  (T2:202-203, step 3's exception, `pr-harden`:293-303). One of its non-blocking findings, r2-2,
  introduced blocking r3-1: #514-2:51 ("-> round 3 showed it falsely accuses … withdrawn · cost: 1 round"),
  :59 ("introduced by a NON-blocking fix (the #465 pattern again)"). Counting the PR's rounds, round 2 was
  the PR's sixth, and `pr-harden`:112 makes a round past the third BLOCKING-ONLY, whose non-blocking
  findings are notes that reach no fixer (:115-118). `pr-harden`:120-128 is that rule's own reason: #465's
  blockers introduced by fixes of non-blocking findings.
- **#514-3.** Started with no entry: T3:22-23 reads the entry for `pwd -P` as `null`, T3:32 writes round
  1. The skill gap it states: #514-3:24 ("'rounds 1-3 are full' does not say what to do when a run adopts
  a PR that has already had many rounds in earlier runs and has no state entry. I ran blocking-only from
  round 1."), :9. It rebuilt the declined ledger by hand from #514-2's record: T3:37 reads the `## Declined`
  sections of `~/.claude/skill-lessons/*514*`, and the round-1 brief (T3:87) carries five declines. It
  converged in 5 rounds (#514-3:2).

Edit (`pr-harden`, a new Step 0 bullet after the adoption bullet that ends at :86):

    - **A PR the pool hands back arrives with no entry to adopt.** `pool-run` clears the gate entry at
      every worktree it creates or removes, so a draft PR routed here by `ticket-pool` finds none. Carry
      forward from that PR's run records in `~/.claude/skill-lessons/` instead: their `## Declined`
      sections are the declined ledger, and their `rounds:` count toward *From round 4 on*, though not
      toward the cap. #514/PR524's second run restarted at a full round 1, and its round 2, the PR's
      sixth, sent a non-blocking finding to a fixer whose fix introduced blocking r3-1.

Two limbs, separable: (a) rounds count per PR for the blocking-only rule, #514-2's cost and #514-3's
stated gap; (b) the records' declines are the ledger, #514-3 only, 0 cost, done by hand.

Prunes: nothing subsumed. Net +6 lines. Minor bump. The cap stays per run on purpose: `--max-rounds N`
is the caller's budget (`pr-harden`:911-913), and no gate script reads `round` against a cap
(`pr-harden-gate.sh` reads it only into messages, :293).

## Parked in the draft (for the refuter to check the counts)

- **An adopted PR's round 1 run before the ticket's newer comments were read: 1 record** (#514-3). The
  owner's decision on #514 was posted 2026-09-24T23:58:35Z, before the loop started. The orchestrator
  first read it at T3:96, after round 1's reviewer returned (T3:90, T3:101). It carried declined item 1
  ("#514 stays open; PR says Refs, not Closes") as settled into the round-1 brief (T3:87), though the
  owner's point 5 says the PR "should say `Fixes #514`". #514-3:17 prices r1 at 1 round, but r1-1 was
  #514-2's open r6-1, which round 1 would have raised anyway (T3:101). The sixth window's #512 park
  (REJECTED.md:4175) is a different mechanism and home: resolve-ticket read the comment and did not carry
  its verification. A pr-harden remedy would not have reached #512.
- **A `transcript:` line naming another real session: 1 record** (#514-3, above). 0 cost to the run.
  Store-wide, measured by `artifacts/2026-09-25-transcript-check.py` (a record's transcript contains a write of
  its own file name). Calibrated on #514-2 (write found) and #514-3 (not found). Of 141 record headers,
  124 carry a transcript line. The only wrong uuid is #514-3's; three other misses were detector false
  positives, checked by hand (#473 writes `$D-…`, and #488-PR501 and #477-PR523 were renamed by retros).
  10 skill-written records name the right uuid under the wrong project folder (#266, #234, #297, #238,
  #229, #340, #355, #357, #409, which the 09-23 retro corrected, and #527). 4 driver captures carry the
  literal `<cwd-slug>` (`pool-run`:3179), beside a `project_dir_name` at :1208. All fail loudly, and all
  but #527 predate 09-14.
- The rest of those 124: #337 and #338 of 09-04 (3 headers) name a uuid found nowhere under
  `~/.claude/projects`, and no transcript there holds their record writes. Whether the uuid was wrong
  or the file is gone is not established.
- Counts advanced: **main moving under a run: 6 records** (+#514-3:10, #540 with 8 conflicting files and
  an ADR 119 collision, a merge agent before round 1, 0 rounds). #514-3:25's "no phase for merging
  before round 1" is true: `pr-harden` merges only at FINISH (:759-766).
- Not advanced, checked: **fix-introduced blocker** stays at 2 records (T3's reviewer reports at :138,
  :178, :217 attribute no blocker to a previous fix). **Prose made false**: the r5 note (#514-3:21) is
  an ADR bullet that does not name case 4. That is an omission, not a false sentence, and the PR body
  names it (T3:273, per `pr-harden`:715-717).
- Observed, no remedy owed: the cap raise on #308's signal (#514-3:9, 4→6 after four different defects,
  converged at 5), `pr-harden`:904-913 as written; and the owner narrowing what blocks ended a
  non-convergence the did-not-converge → draft path had handed back (#514-3:7).

## The gate's reply, round 1 (fresh read-only agent; its verdicts, condensed, with its citations)

- **P1 → REVISE, as a deletion.** Blocking: bar (c) fails, since :41 governs reading and :34-35
  counting, and a case the key misses is a GAP (REJECTED.md:330-333, :1103, :1972-1975). The key came
  from the fifth window's gate measuring a different-PR append (:4116-4119). The defect is real, and a
  Step 1 correction has precedent (:3060-3067). Deleting a false clause needs no bar (:2454-2455).
  Blocking: the drafted "name each by its file and header line rather than its PR" re-counts #514-1,
  which :4100 names only as "#514/PR524", and replacement text needs a calibrated measurement
  (:3646-3648). Revised text: "so count only the records the ledger does not already name." Net 0.
- **P2(a) → REVISE.** Holds: T2:22-23/:32, T2:134/:195, T2:202-207, #514-2:51/:59, T3:22-23/:32, and
  `pool-run` clearing at every worktree it creates or removes (:1146, :1200, :3243, :3353; `worktree
  add`/`remove` only at :2390/:2410). Blocking: "a PR the pool hands back / routed here by
  `ticket-pool`" is wrong. The driver skips an open-PR ticket as `has-open-pr` (`pool-run`:167,
  :2117-2125), and the only `/pr-harden` in `pool-run` is what `--claim` prints (:4055). T2 and T3 were
  `/pr-harden 524` sessions an operator session's chain script ran after `pool-run --claim 514`
  (`d2089475…jsonl`:1701, :1761, :2106). Blocking: the rule's own home, :112-113 ("Otherwise rounds 1 to
  3 are full rounds") and the diagram at :96, would stand as universals the new rule falsifies. That is
  the 09-23 gate's blocking on this same phrase (REJECTED.md:3821-3827). Non-blocking: the proposal
  answers :4414-4420, it does not re-propose it. It should state the cost, as :3649-3651 did: r2-1 and
  r2-3 become notes, and on round 2's count of 0 the run ends there. "Not toward the cap" is a new
  rule. Records are a fragile source, so read the PR description too, and match records by the PR in
  their header rather than a ticket glob.
- **P2(b) → PARK.** One record, 0 cost, and #514-1's `## Declined` was "none" (#514-1:30). T3's fifth
  carried item came from the PR body (T3:33, "(round-5 note)"), not from a `## Declined` section.
  #514-2's record omits that note (T2:369, T2:437). FINISH makes the description the home of every
  finding not implemented (`pr-harden`:715-717). **REOPEN ON:** a run adopting an entry-less PR that
  re-raises, or is re-given, an earlier decline or note at a cost of a round; or a second hand rebuild.
- **Parked-list corrections:** "all but #527 predate 09-14" is false, since `…-515-driver.md` is 09-24.
  "Merges only at FINISH" is false, since Step 1 :193-230 covers in-loop merges; the true point is that
  :193 compares against "the one the previous round saw". #514-2:48 is a second before-round-1 merge
  record. The detector tests a MENTION, not a write. A write-based re-run flagged #377 and #471 as well,
  and both are real writes by hand, so "only wrong uuid" stands. The units are mixed: "141 record
  headers" includes notes, and "10 records" is 11 headers.
- **Its own findings:** `ticket-pool`:51 ("hands it to `pr-harden`") against the driver and against
  `ticket-pool`:46/:394; #514-2's record is missing its round-5 note; this is a one-record window.

## Staged after round 1 (querystore working tree, for round 2)

P1 as revised. P2(a) as revised, with three changes of mine, each checked:
- "real holes that fixer implemented in part" in place of "closed" (T2:208: `"implemented": ["r2-1",
  "r2-2", "r2-3"]`, with r2-1 and r2-3 also partly declined as "Implemented in a narrower form");
- "toward *1 — REVIEW*'s round 4" in place of "toward *From round 4 on*", since the bold phrase it named
  now reads "From the PR's round 4 on";
- the counter-case clause "on round 2's recorded count of 0 the run would have ended there" (T2:203).
Plus `ticket-pool` 0.24.3, correcting :51. The printed command takes the PR number (`pool-run`:4055),
so the text says `--claim <ticket>` and `/pr-harden <pr>`, not one `<n>`. Linter 10 files, 0 findings on
the staged copies; `pool-test.py` 580 passed / 0 failed on the staged tree.

## The gate's reply, round 2 (a second fresh read-only agent, over the staged diff)

- **P1 → APPLY.** It counts #514-3 and not #514-1 (REJECTED.md:4100) or #514-2 (:4379). Non-blocking:
  "the ledger" was undefined, and `ticket-pool` uses the word for `ledger.json` (:53, :91), so it
  offered "`REJECTED.md`". This window's heading must name #514-3 as the THIRD loop.
- **P2(a) Step 0 bullet → REVISE.** Blocking: the cost clause left out that ending at round 2 skips
  rounds 4 and 5, which found r4-1 (T2:314) and r5-1 (T2:369), false reports whose code paths were
  already at round 2's head `7549b068`, and which round 2's reviewer did not raise (T2:196). See also
  REJECTED.md:4392-4393 and :4415-4416. Non-blocking: "every worktree it creates or removes" and "a PR
  worked again in a pool worktree" were both too broad. `make_worktree` removes a leftover and
  `worktree add` can then fail (`pool-run`:2386, :2390-2392); a kept worktree keeps its entry
  (:1199-1200). Non-blocking: when sources are missing or disagree (a driver capture's `rounds:
  unknown`; T3:33's description naming only the second loop), park it. Non-blocking: Step 0 carries
  `round` forward (:73), but the next paragraph writes `round: 1`, so it offered "and `round: 1` unless
  you adopted an entry, whose `round` you keep". Verified against the transcripts: T2:32/:134, #514-1:3,
  T2:196/:208/:245, T2:203, T2:137.
- **P2(a) :96 and *1 — REVIEW* → APPLY.** No third home, found by a grep of five skills, both gate
  copies and gate-state. Non-blocking, parked with no edit: step 3's exception is "at most once per run"
  (:302); "every round after a clean full round" (:121) and "From round 2 on … re-derive" (:256) still
  count per run.
- **ticket-pool → REVISE.** "Skips it as `has-open-pr`" holds: it drove the real `consider` with the
  ledger's `pr` set. Blocking: the `--claim` clause is false for a PR only the ledger can find.
  `cmd_claim` calls `pr_for_ticket` without `since` (`pool-run`:4019), which matches on title, branch or
  closing keyword (:2025). Driven with a numberless branch and `Refs #514` in the body, it returned
  None, and `--claim` prints `/resolve-ticket` (:4057). Revised to the skip clause alone.
- **Else:** "Implemented in a narrower form" is the reason recorded for r2-1 only (T2:208). The
  transcripts are UTC: T3 runs 02:25Z to 07:30Z on 09-25. P2(a) widens the round-4 rule, which the
  ledger records as owing its own refutation pass (REJECTED.md:3687-3691), and that reopen is not
  met.

## Applied

All of round 2's texts as given, including both offered clauses, plus moving the lone "A" at the end
of *1 — REVIEW*'s first paragraph onto the next line. `pr-harden` 0.33.0, `skill-retro` 0.2.11,
`ticket-pool` 0.24.3. Net: pr-harden +11, skill-retro 0, ticket-pool 0. Linter 10 files, 0 findings;
`pool-test.py` 580 passed / 0 failed, run on the first staging.

And on the final tree: `pool-test.py` 580 passed / 0 failed.

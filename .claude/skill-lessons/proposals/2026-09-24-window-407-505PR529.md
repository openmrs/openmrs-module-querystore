# Proposals — 2026-09-24 seventh window: #407/PR526, #505/PR529

## P1 — pr-harden: a delegated agent waits inside its turn (verifier brief contradicts the orchestrator rule)
Bar: (c) self-contradiction, plus (a) two records (#238 2026-08-29:29 verifier yielded on a boot
notification; #407 2026-09-24 reviewer returned "waiting for notifications" mid-build, ~20 min).
Contradiction: pr-harden verifier brief *Wait on a CONDITION, never on a clock* prescribes
`Bash(run_in_background: true)` "which hands the turn back at once and notifies you when it exits";
*And this session must not busy-wait either* says "Backgrounding the wait and ending the turn for its
notification is what the gate refused" and prescribes a bounded foreground loop.
Edits:
 a) verifier paragraph: replace the backgrounded-loop sentence with a foreground loop bounded under the
    tool timeout (pointing at *this session must not busy-wait either*), plus "do not background it and
    end the turn: a delegated agent that ends its turn has returned its report unfinished" citing #238/#407.
 b) delete the following *The harness disagrees with itself here* paragraph (its whole content argues for
    background Bash + single notification, which (a) retires).
 c) *Tell every agent to restore BEFORE it reports*: add "and to wait on its own builds inside its turn".
Prunes: paragraph (b), ~7 lines.

## P2 — harden: the KIND-of-question rule gets a trigger — the second escape
Bar: (a) #482 (park "A shipped KIND-of-question rule not applied across three Phase-2 passes: 1 record")
and #505/PR529 ("The KIND-change rule fired late (after ~6 constant shapes) — cost several cycles";
regex constant parser, ~3 harden cycles). Also (b) within #505.
Edit: after the bullet's bold opening sentence add: "The trigger is the SECOND escape: when a fresh
agent defeats a guard your fix already widened once, the next fix changes the kind. #482 waited three
Phase 2 passes, and the change came from a pr-harden fixer; #505's constant parser met about six shapes
over ~3 cycles before it asked the class file's ConstantValue instead."
Prunes: nothing. +3 lines.

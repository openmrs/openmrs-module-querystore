# Proposals — 2026-09-24 ninth window: #273/PR533, #430/PR532

## P1 · pr-harden 0.32.1 (patch) · bar (c): skill contradicts itself
Step 4 (`pr-harden/SKILL.md`, "**A finding may name the PR DESCRIPTION rather than a file, and it can be
blocking.**") says the orchestrator applies it, "it still counts as the round's fix and the round proceeds
normally". Such a round pushes no commit, so the next round's step 1 head-equality guard ("If the new head
EQUALS the previous round's … stop and establish why before spawning: the round pushed no commit (the fixer
declined everything — that is a *did not converge* …), or the push had not landed …, or the ref was created
outside a round at all") fires and offers no cause that fits, the nearest one reading as a stop.
Record: #273:22 "a blocking finding on the PR DESCRIPTION leaves the head unmoved, so the confirming round
reviews the same sha; step 1's 'identical sha' guard reads as a stop, resolved by noting the body changed".
Step 3 already routes the NON-blocking description-only case ("they named only the PR description — no
round is owed"); the blocking case is the one gap.

Edit: append to the step-1 cause list, before "or the ref was created outside a round at all":
"or the round's one fix was a blocking PR-description finding the orchestrator applied (step 4), which
leaves the head where it was and is still owed its round — brief the reviewer that only the description
changed;"
Prunes: nothing; +2 lines. Justified as closing a contradiction, not adding a rule.

## P2 · resolve-ticket 0.21.1 (patch) · bar (a) via the park at REJECTED.md "Orchestrator yield on a
background build/Monitor refused by the Stop gate, AFTER pr-harden 0.30.0's foreground-loop rule: 1 record"
(#429:18), whose REOPEN ON is "a second post-0.30.0 record". #273:19: "resolve-ticket Step 6 — a background
root build … the Stop hook refused the yield while waiting on it (monitor-based wait), recovered with a
foreground until-loop". The foreground-loop rule lives only in pr-harden (*And this session must not
busy-wait either*); resolve-ticket Step 6 (Green), where #273 met it, says nothing about waiting.

Edit: append to resolve-ticket Step 6:
"Wait on it inside the turn with `pr-harden`'s bounded foreground loop (*And this session must not
busy-wait either*); the gate armed at Step 1 refuses a yield on a backgrounded build here too (#273)."
Prunes: nothing; +2 lines, a pointer rather than a restatement.

Not edited, reported again: `~/.claude/CLAUDE.md` *Wait on a condition* still steers to
`run_in_background` + completion notification.

## Gate reply (fresh general-purpose agent, read-only)
- P1: PARK. BLOCKING — re-proposes REJECTED.md:1972-1980's kill (PR393:40-46, same two sentences); a gap,
  not a contradiction; #273:23 resolved at zero cost, so that park's REOPEN ON is not met. Citation was
  :22, is :23. Readmission wording if reopened: "or the round's only fixes were PR-description findings
  the orchestrator applied (step 4), which leaves the head where it was and still owes its round; brief
  the reviewer that only the description changed".
- P2: APPLY-WITH-REVISION. Citation :19 → :21. #429:18 names no step, so the pointer moves from Step 6
  to the autonomy contract's *The Stop gate covers the whole run* paragraph, covering every step. The
  bar rests on (a); the "one stale build" cost is the early build start, not the refused yield.

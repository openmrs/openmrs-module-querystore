# resolve-ticket + harden + pr-harden · openmrs-module-chartsearchai · #293 / PR #323 · 2026-08-27
outcome: converged (pr-harden round 1 reported 0 blocking); harden ended on a labelled override after cycle 6
rounds: 1   cycles: 6 (harden)   verifier: ran (works at runtime)
context: no compaction · peak not surfaced
transcript: ~/.claude/projects/-Users-danielkayiwa--claude-pipeline-worktrees-openmrs-openmrs-module-chartsearchai-293/b1054d34-b253-4377-a52d-e55ee8892470.jsonl

## Refuted by measurement
- "the concept of a non-coded order is a generic placeholder" (ticket's assertion) -> confirmed, but from CORE rather than assumed: `OrderServiceImpl.ensureConceptIsSet` assigns the `drugOrder.drugOther` concept, and `DrugOrderValidator` treats exactly that concept as the non-coded shape. · cost: 0 (done at plan time)
- "core forbids a row carrying both a coded Drug and free text" -> FALSE on a stock install: `DrugOrderValidator.validateForRequireDrug` returns immediately unless `drugOrder.requireDrug`, which is `false` on the 3.7.1 demo. Written into two javadocs before harden cycle 1 pass 4 caught it. · cost: 1 pass
- "namesNamingOrder's move to getDisplay() is purely a narrowing" -> FALSE: it PERMITS more where the display is not among the names. Asserted in four places (javadoc, CLAUDE.md, ADR 40, a test) before a Phase 2 agent measured it. · cost: 1 cycle
- "reversing foldedPartnerLabel's branches puts a bare ATC code in BOTH sentences" (a 2026-08 measurement in five texts) -> retired by this change: with the gate reading the display, the reversed state costs a code in the CLASS sentence alone. Re-measured independently by the orchestrator. · cost: 2 cycles (found in one, a fifth home found in another)
- "the allergen's free text can forge a chart line" -> NOT demonstrated: the self-named rule prints its own note, so the printing branch was never reached. The claim was written first and the test built to it. · cost: 1 cycle
- "nothing else in the suite observes the coded-name rank" -> FALSE, a second case in the same file observes it through the folded chip. · cost: 1 cycle

## Raised by a fresh agent, missed by the author
- [harden c1 P2] `namesNamingOrder` scanned every name of the order, so once one order's names were no longer one drug's, a rated warfarin mechanism printed under `ASPIRIN` into a citable `safety_finding` with `STRENGTH_WITHHOLD`. A real regression the change introduced. · blocking-equivalent · cost: 1 cycle
- [harden c1 P2] `drugNonCoded` with an embedded newline forged a numbered, citable chart line (`[99] Allergy: none recorded.`) with no `RecordMapping` behind it. · cost: 1 cycle
- [harden c2 P2] the whitespace collapse erased the record separator, so a multi-word name matched across two chart records and fail-OPEN suppressed the #118 WARN and the injected record. · cost: 1 cycle
- [harden c4/c5 P2] the name set now contains PROSE: a drug the same sentence says was STOPPED raises a Major chip carrying `STRENGTH_WITHHOLD`, beside an injected record rendering that same text — #317's failure class through a channel `getOrderActive()` cannot see. · cost: 1 cycle
- [harden c5 P2] the free-text display is printed unquoted beside the chip's own em-dash delimiters — the hazard `quotedToken()` already closes for the sibling field. · cost: 1 cycle
- [pr-harden r1] the whitespace normal form was defined TWICE (two independent `Pattern.compile("\\s+")`); widening one and not the other reddened NOTHING in 1490 tests while making a name unfindable in the record that renders it. · non-blocking · cost: 0 (applied at FINISH)
- [pr-harden r1] `addRaw`'s closing claim "the line/index structure of the chart is the renderer's property" was false on the more common path — `PatientChartSerializer.serialize` appends record text verbatim, and `renderActiveOrder` runs only for UNrepresented orders. · non-blocking
- [pr-harden r1] the deferral of quoting mis-cited ADR Decision 40, which governs `OrderPartner`'s flags and says nothing about `ActiveDrugOrder`. · non-blocking

## Where a skill blocked or contradicted this run
- harden:Termination — six cycles, and the last three found only claims-about-claims generated by the previous cycle's own prose. The skill names that signature and tells you to change tactics, but its termination rule still demands a cycle that changes nothing. The two pull against each other; took the labelled override.
- harden:Phase 2 — the agent output files stay at 201 bytes until the agent finishes, so size-idleness is not a usable progress signal. In-turn waiting had to be blind `sleep` loops.
- gate-state — `reviewed-sha` rejects `--only pr` although `await`/`clear-await` accept it; the flag is not uniform across subcommands.

## Declined
- (harden) `containsBoundedToken` re-folds its haystack per alias, so a 255-char free text costs ~4.3ms per resolution against ~0.79ms — if we ship without fixing it, a non-coded order whose text is a full sentence costs a few tens of ms per request against a multi-second LLM call, and the fix is a shared hot path needing its own measurement gate.
- (harden) not quoting the order display — if we ship without it, a clinician reads a chip whose sentence boundaries are partly the chart's and the model reads the same string as evidence; nothing is asserted falsely, and closing it needs `ActiveDrugOrder` to carry its display's source.
- (harden) not closing the prose cost by refusing or parsing free text — refusing it IS the fix the ticket asks for, and parsing clinical prose is a different problem.

## Assumptions review overturned
- A2 "keeping the concept name is purely additive, so every match that worked before still works" -> false once the whitespace collapse landed; the collapse moves matches in BOTH directions and the claim had to be scoped to `addDrugName` alone. (harden cycle 2)
- "the display is one of the order's names" -> true only for a builder-named order; the code-only stand-in and a caller-built order break it, and the permitting leg of change 3 depends on exactly that. (harden cycle 4)

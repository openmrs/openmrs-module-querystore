# resolve-ticket / pr-harden · openmrs-module-querystore · #81 / PR #82 · 2026-09-23
outcome: converged
rounds: 1   cycles: 1 (harden: 2 Phase 1 passes, 1 Phase 2)   verifier: ran (works at runtime)
context: no compaction · peak not surfaced
transcript: ~/.claude/projects/-Users-danielkayiwa-Projects-openmrs-querystore/f9ee6080-1bad-4ce7-9d22-9bd70c22b6f9.jsonl

## Refuted by measurement
- The planned Kiritimati (+14) test with a 1962 birthdate would fail on the pre-fix code -> it passed; Kiritimati was UTC-10:40/-10 before 1995. Moved to a 2015 date. · cost: none (caught at red-test step)
- The plan kept a `java.sql.Date` instanceof branch in formatCalendarDate as necessary -> deleting it left all tests green; `Instant.ofEpochMilli(getTime())` suffices. Deleted. · cost: none (harden pass 1)
- Existing UTC-noon birthdate fixture would stay host-independent -> under -Duser.timezone=Pacific/Auckland the ADR-example test failed with `Born 1982-07-15`; added DateFixtures.localMidnight. · cost: one harden pass

## Raised by a fresh agent, missed by the author
- [gate] event path projects the in-memory entity, not a JDBC re-read, so "JDBC materializes at local midnight" doesn't describe sync · non-blocking · settled by the verifier (REST create + update matched the DB)
- [harden P2 quality] ADR said "every server east of UTC" — false universal; the zone's offset on the date is what matters · non-blocking · cost: one edit

## Where a skill blocked or contradicted this run
- resolve-ticket Step 3 — `gate-state await` without `--run` errors when it reaches the harden entry; needed `--only pr` for the pre-harden awaits. Cost: two tool calls.
- resolve-ticket Step 8 title voice (`type(scope): behaviour`) does not match this repo's code-PR titles (imperative "Add …"); followed the repo.

## Declined
- none

## Assumptions review overturned
- none
